# BrandSpace — Decision Register

> **الملخص التنفيذي بالعربية**
>
> هذا المستند هو **سجل القرارات** ومركز المراجعة لمالك المنتج. وهو مقسّم إلى أربعة أقسام واضحة:
>
> 1. **افتراضات معتمدة** — مأخوذة مباشرة من متطلبات المالك، لا تحتاج نقاشًا.
> 2. **قرارات موصى بها** — اختيارات تقنية اتخذها المهندس المعماري مع الأسباب والبدائل؛ تُطبَّق ما لم يعترض المالك.
> 3. **قرارات منتج غير محسومة** — تحتاج بحثًا أو تجربة أو معلومات إضافية قبل الحسم.
> 4. **قرارات تتطلب موافقة المالك قبل البدء** — **لا تبدأ المرحلة الأولى من التنفيذ قبل الإجابة عليها**.
>
> **الأهم:** القسم الرابع يحتوي على القرارات التي تحجب التنفيذ فعليًا — خصوصًا: مزودو الذكاء الاصطناعي، هيكل الخطط والأسعار،
> قيمة الرصيد وسياسته، مزود الدفع والأسواق المستهدفة، وأولوية منصات التواصل الاجتماعي.
> يُرجى مراجعة القسم الرابع أولًا.

---

## How to Use This Document

| Section                         | Meaning                                                  | Action needed                                    |
| ------------------------------- | -------------------------------------------------------- | ------------------------------------------------ |
| §1 Approved assumptions         | Stated in the owner's requirements; treated as settled   | None — correct me if any is wrong                |
| §2 Recommended decisions        | Engineering choices with reasoning and alternatives      | Accept, or amend; silence is taken as acceptance |
| §3 Unresolved product decisions | Genuinely open; needs research, data, or a business call | Answer when ready; does not block Phase 1        |
| §4 Requires owner approval      | **Blocks implementation**                                | Answer before Phase 1 begins                     |

Each entry has an ID (`A-`, `R-`, `U-`, `D-`) so it can be referenced in code reviews and commits.
When a decision is made, record the date and the choice here — this file is the project's memory.

---

## 1. Approved Assumptions

Taken directly from the product requirements. These are settled and shape everything else.

| ID   | Assumption                                                                                                                                                                                                                                                                                          |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A-01 | BrandSpace is a **bilingual Arabic/English** platform; Arabic RTL is first-class, not a translation layer                                                                                                                                                                                           |
| A-02 | The platform is **multi-tenant**; every customer belongs to an isolated **Workspace**                                                                                                                                                                                                               |
| A-03 | There are **three architecturally separated interfaces**: public website, customer dashboard, and Platform Admin                                                                                                                                                                                    |
| A-04 | Platform Admin is for the BrandSpace owner and authorized internal staff only, with platform-level permissions and future 2FA (this design makes 2FA mandatory for Owner and Admin from Phase 2)                                                                                                    |
| A-05 | A user in Workspace A must never read, modify, search, export, or **infer** data from Workspace B                                                                                                                                                                                                   |
| A-06 | The platform is **configuration-driven**; the owner runs normal operations without editing code                                                                                                                                                                                                     |
| A-07 | Providers, models, routing, plans, prices, limits, credit costs, feature availability, integration credentials, templates, billing config, currencies, trial duration, usage limits, and default policies are **never hard-coded**                                                                  |
| A-08 | Configuration is **versioned** with validation, activation, rollback, change history, and audit logging                                                                                                                                                                                             |
| A-09 | Secrets are encrypted at rest, never exposed to frontend code, never in logs or responses, and readable only as masked metadata                                                                                                                                                                     |
| A-10 | The AI Gateway is **provider-agnostic**; the customer never sees the platform API key                                                                                                                                                                                                               |
| A-11 | **Optional customer BYOK** is available in higher plans                                                                                                                                                                                                                                             |
| A-12 | AI credits abstract away provider tokens; failed requests are never charged; retries never double-charge                                                                                                                                                                                            |
| A-13 | Customers connect social accounts via **OAuth only** — BrandSpace never asks for a social password                                                                                                                                                                                                  |
| A-14 | Platform Admin holds the social platform application credentials                                                                                                                                                                                                                                    |
| A-15 | Only approved content is published when approval is required                                                                                                                                                                                                                                        |
| A-16 | Every external publish, delete, disconnect, or bulk operation requires a confirmation policy and an audit record                                                                                                                                                                                    |
| A-17 | The AI Copilot is permission-aware and must never silently publish, delete, disconnect, pay, or send external communications                                                                                                                                                                        |
| A-18 | The architecture must not be bound to a single payment provider                                                                                                                                                                                                                                     |
| A-19 | The MVP is a **modular monolith** that can be separated into services later; unnecessary microservices are avoided                                                                                                                                                                                  |
| A-20 | ~~Brand colors are Blue `#00ADEE` and Yellow `#FFDD15`~~ — **SUPERSEDED by D-42, D-60 and D-61.** The brand colours are Purple `#7935FE` and Yellow `#FFDD15`, everywhere including the public website; blue is retired. The original assumption is kept verbatim because it was true when recorded |
| A-21 | The first vertical slice uses **mock providers** and excludes real social publishing                                                                                                                                                                                                                |
| A-22 | ~~Customer types supported: individual founder, startup, company marketing team, creator, agency, enterprise team~~ — **NARROWED by D-62.** The MVP serves businesses, founders, in-house brand teams and multi-brand companies. Agencies are possible future expansion, outside MVP scope          |
| A-23 | Customer roles: Workspace Owner, Workspace Admin, Marketing Manager, Content Creator, Copywriter, Designer, Approver, Analyst, Viewer (read-only) — stored as `client_viewer`, relabelled by D-58                                                                                                   |
| A-24 | Platform roles: Platform Owner, Platform Admin, Support Agent, Billing Manager, Operations Viewer                                                                                                                                                                                                   |
| A-25 | Permissions exist at platform, workspace, brand, and campaign levels                                                                                                                                                                                                                                |
| A-26 | Automated tests must prove tenant isolation                                                                                                                                                                                                                                                         |
| A-27 | Implementation does not begin until these architecture documents are reviewed and approved                                                                                                                                                                                                          |

---

## 2. Recommended Decisions

Engineering choices made in this phase. Each states the decision, the reasoning, the alternatives rejected,
and what it would cost to change later.

| ID   | Decision                                                                                                      | Reasoning                                                                                                                     | Rejected alternatives                                                                                            | Cost to reverse                   |
| ---- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| R-01 | **Modular monolith + monorepo**                                                                               | Small team velocity, transactional integrity across billing/credits, single deploy; boundaries preserved for later extraction | Microservices from day one (operational overhead, distributed transactions); separate repos (version skew)       | Low — designed for extraction     |
| R-02 | **Shared database, shared schema, `workspaceId` + RLS**                                                       | Scales to self-serve sign-up; RLS gives defense in depth; enterprise-dedicated instances remain possible                      | Schema-per-tenant (migration cost explodes); database-per-tenant (operationally heavy)                           | High — change early or not at all |
| R-03 | **TypeScript everywhere** (Next.js, Node API, Node workers)                                                   | Shared types, schemas, and domain packages across web/api/worker                                                              | Polyglot backend (Go/Python) — doubles toolchain, loses type sharing                                             | High                              |
| R-04 | **PostgreSQL 16+** as the only primary datastore                                                              | Relational integrity for money, JSONB for config, RLS for isolation, pgvector for embeddings                                  | MySQL (no RLS), MongoDB (wrong for financial invariants)                                                         | High                              |
| R-05 | **Prisma** with a tenant-scoped client extension                                                              | Best-in-class TS ergonomics + migrations; scoping enforced by construction, not by memory                                     | Drizzle (better SQL control, thinner tooling), raw SQL (unacceptable human-error risk)                           | Medium                            |
| R-06 | **pgvector** for Brand Brain, not a dedicated vector DB                                                       | Embeddings inherit the same RLS tenant isolation; MVP volumes are small                                                       | Pinecone/Weaviate/Qdrant — second, weaker isolation boundary and another vendor                                  | Low — extractable later           |
| R-07 | **Redis + BullMQ** for queues, delayed jobs, locks, rate limits                                               | Delayed jobs are essential for scheduling; local-dev parity; no cloud lock-in                                                 | SQS/Cloud Tasks (lock-in), Temporal (heavier; revisit at Phase 6 for publishing sagas), pg-boss (fewer features) | Low                               |
| R-08 | **Database is the source of truth for schedules**; the queue is an accelerator, with a reconciliation sweeper | A Redis outage must cost punctuality, not correctness                                                                         | Queue-only scheduling                                                                                            | Low                               |
| R-09 | **S3-compatible object storage** behind a `StorageProvider` interface, pre-signed uploads, no public buckets  | Vendor-swappable; API never proxies bytes; security by construction                                                           | Direct SDK coupling; serving files from the app origin (XSS risk)                                                | Low                               |
| R-10 | **Zod** as the single schema language (HTTP, jobs, webhooks, config, AI output, env)                          | One definition reused front and back; config validation gets it for free                                                      | Separate validators per layer; JSON Schema (worse TS ergonomics)                                                 | Low                               |
| R-11 | **Auth.js** with two isolated realms + custom RBAC                                                            | Fast to build, covers OAuth/credentials; realm separation is ours, not the library's                                          | Custom auth from scratch (risk), a hosted IdP now (cost and lock-in before product-market fit)                   | Medium                            |
| R-12 | **2FA mandatory for Platform Owner and Platform Admin from Phase 2** (stronger than "future support")         | Admin compromise is catastrophic; TOTP is cheap to add now                                                                    | Optional 2FA at launch                                                                                           | Low                               |
| R-13 | **Vitest + Testcontainers + Playwright + k6**; isolation suite is blocking in CI                              | Real Postgres/Redis in tests is the only way to prove RLS behavior                                                            | Mocked database tests (would not prove isolation)                                                                | Low                               |
| R-14 | **OpenTelemetry**, vendor-neutral export                                                                      | Avoids observability vendor lock-in; consistent trace context across API and workers                                          | Vendor-specific agents                                                                                           | Low                               |
| R-15 | **Separate `apps/admin` deployment on a dedicated hostname**                                                  | Hard separation of session realm, permissions, and blast radius                                                               | Admin routes inside the dashboard                                                                                | Medium                            |
| R-16 | **Reserve → confirm → settle** credit model with an immutable ledger and a materialized balance               | The only design that satisfies "no charge on failure", "no double charge", and "never negative" under concurrency             | Charge-after-success (overspend under concurrency); charge-before (refund complexity)                            | High                              |
| R-17 | **FIFO-by-expiry credit consumption**                                                                         | Customers are never surprised by credits expiring that they could have used                                                   | LIFO; proportional consumption                                                                                   | Low                               |
| R-18 | **Downgrades take effect at period end; upgrades immediately**                                                | Avoids refund complexity and abrupt capability loss; standard SaaS expectation                                                | Immediate downgrade with proration                                                                               | Low                               |
| R-19 | **Hosted checkout and hosted billing portal**                                                                 | Keeps PCI scope minimal; card data never touches BrandSpace                                                                   | Custom payment forms (PCI burden)                                                                                | Low                               |
| R-20 | **Entitlements resolved from our own `Subscription`/`Plan`**, never directly from webhooks                    | A provider outage or spoofed event can never change access                                                                    | Provider as the entitlement source of truth                                                                      | Medium                            |
| R-21 | **Uncertain publish outcomes are verified, never blindly retried**                                            | The single most important rule for avoiding duplicate posts                                                                   | Retry on timeout                                                                                                 | Low                               |
| R-22 | **Copilot tool calls are authorized server-side per call**; the model's claims are ignored                    | Prevents prompt-injection privilege escalation                                                                                | Trusting the agent's plan                                                                                        | Low                               |
| R-23 | **Raw AI prompts and completions are not persisted by default**                                               | Privacy by default; audit-safe summaries suffice for support and billing                                                      | Full logging by default                                                                                          | Low                               |
| R-24 | **Mock adapters for AI and social are first-class and permanent**                                             | Enables the whole vertical slice, CI, and local dev without real credentials                                                  | Real providers first                                                                                             | Low                               |
| R-25 | **404, not 403, for cross-tenant access**, with identical shape and timing                                    | Prevents existence inference                                                                                                  | 403 (leaks existence)                                                                                            | Low                               |
| R-26 | **Cursor-based pagination** on all tenant data                                                                | Correctness under concurrent writes; no deep-offset cost                                                                      | Offset pagination                                                                                                | Low                               |
| R-27 | **Expand/contract migrations**, deploys always backward-compatible                                            | Zero-downtime deploys; safe rollback                                                                                          | Breaking migrations with downtime windows                                                                        | Low                               |
| R-28 | **Single region at MVP**, region selection deferred                                                           | Complexity and cost are not justified before demand exists                                                                    | Multi-region from day one                                                                                        | Medium                            |
| R-29 | **Agency support via both models** (many brands in one workspace, or one workspace per client)                | Different agencies want different isolation/billing shapes; the data model supports both with no extra machinery              | Forcing one model                                                                                                | Low                               |
| R-30 | **True impersonation is prohibited at MVP**; support acts as a labeled platform actor                         | Impersonation destroys audit clarity and is the most abusable feature in any SaaS admin                                       | "Log in as user"                                                                                                 | Low                               |
| R-31 | **Dual control on financial configuration** (pricing, credit costs): one actor drafts, another activates      | Prevents single-actor pricing mistakes and insider risk                                                                       | Single-actor activation                                                                                          | Low                               |
| R-32 | **Tailwind + Radix** for the design system                                                                    | Native RTL support via logical properties; accessible unstyled primitives                                                     | Component libraries with opinionated, hard-to-RTL styling                                                        | Medium                            |

---

## 3. Unresolved Product Decisions

Open questions that need research, data, or a business call. **None of these block Phase 1.**

| ID   | Question                                                                                                                                | Why it is open                                                                  | When it must be answered      | Recommendation                                                                                                                      |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| U-01 | ~~What exactly is 1 AI credit worth to a customer?~~ **ANSWERED provisionally by D-07/D-12.**                                           | Needs a price point that is understandable and defensible                       | Before Phase 3 pricing config | Anchor credits to a round money value (e.g. 1 credit ≈ a small fixed amount) and set task costs so a typical caption costs 1 credit |
| U-02 | ~~Do we allow credit rollover, and how much?~~ **ANSWERED by D-12:** capped at 1× monthly allowance.                                    | Affects perceived value vs. revenue predictability                              | Phase 3                       | Capped rollover (e.g. up to 1× monthly allowance) — generous enough to feel fair, bounded enough to protect revenue                 |
| U-03 | Is BYOK a discount, a higher-tier perk, or both?                                                                                        | Affects plan positioning and margin                                             | Phase 4                       | Enterprise/top-tier perk with a reduced platform fee, not a discount on lower tiers                                                 |
| U-04 | Agency white-labeling — how far? **Moot for the MVP under D-62** (not an agency product); revisit only if agency expansion is approved. | Significant build cost; affects pricing tier design                             | Post-launch                   | Defer; validate demand with early agency customers first                                                                            |
| U-05 | ~~Is there a free tier, or trial-only?~~ **ANSWERED by D-09:** trial-only, no permanent free plan at launch.                            | Affects acquisition, support load, and abuse exposure                           | Before Phase 8                | Trial-only at launch (14 days, no card); revisit a free tier once unit economics are measured                                       |
| U-06 | Read-only Viewer approval rights — can a viewer approve content?                                                                        | Affects agency workflows                                                        | Phase 5                       | Make it an optional per-brand policy, off by default                                                                                |
| U-07 | Are custom workspace roles needed, or are the nine fixed roles enough?                                                                  | Enterprise ask; adds permission-model complexity                                | Post-launch                   | Fixed roles at launch; custom roles as an enterprise feature later                                                                  |
| U-08 | Content retention default for the customer's own drafts and published history                                                           | Storage cost vs. customer expectation                                           | Phase 5                       | Unlimited for active subscriptions; 30-day grace after cancellation                                                                 |
| U-09 | Do we store AI-generated images indefinitely?                                                                                           | Storage cost grows fast                                                         | Phase 5                       | Count against the plan's storage quota, same as uploads                                                                             |
| U-10 | Which analytics metrics do customers actually care about most?                                                                          | Drives dashboard design and ingestion priorities                                | Phase 7                       | Interview 5–10 target customers during Phases 5–6                                                                                   |
| U-11 | Arabic dialect handling in generation (MSA vs. Gulf/Egyptian/Levantine)                                                                 | Materially affects output quality and perceived value                           | Phase 5                       | Make dialect a Brand Brain tone attribute; default to MSA; evaluate model quality per dialect                                       |
| U-12 | Does the Copilot get a conversational memory across sessions?                                                                           | Privacy and cost implications                                                   | Phase 7                       | Per-brand memory, workspace-scoped, customer-clearable                                                                              |
| U-13 | How aggressive should automations be allowed to be (auto-publish without a human)?                                                      | Trust and liability                                                             | Phase 7                       | Require an explicit, separately-granted standing authorization per rule, off by default                                             |
| U-14 | Public template gallery — curated by BrandSpace, or community-contributed?                                                              | Moderation burden                                                               | Post-launch                   | Curated at launch                                                                                                                   |
| U-15 | Status page — self-hosted or third-party?                                                                                               | Cost vs. independence (a self-hosted status page can go down with the platform) | Phase 8                       | Third-party, so it survives our own outages                                                                                         |
| U-16 | Do we need an in-product support chat at launch?                                                                                        | Support load and cost                                                           | Phase 8                       | Email + in-app ticket form at launch; chat once volume justifies it                                                                 |

---

## 4. Decisions Requiring the Product Owner's Approval

**These block implementation. Phase 1 should not begin until §4.1–§4.5 are answered, and each later phase
needs its own items answered before it starts.**

### 4.1 Blocking Phase 1 (Foundations)

| ID       | Decision needed                                                            | Options                                                                                                   | Recommendation                                                                                                                                                                      | Impact if wrong                                                                               |
| -------- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| **D-01** | **Approve the technology stack** (§2, R-01 to R-14, R-32)                  | Accept as recommended · amend specific choices · request alternatives                                     | Accept as recommended                                                                                                                                                               | Changing R-02 (tenancy) or R-04 (database) later is a rewrite; the rest are cheaper to change |
| **D-02** | **Hosting and deployment target**                                          | Managed platform (Vercel + managed Postgres/Redis) · a single cloud (AWS/GCP/Azure) · a regional provider | Managed platform for MVP: lowest operational load for a small team; migratable because everything is containerized and standard                                                     | Affects cost, latency to target markets, and compliance posture                               |
| **D-03** | **Primary data region**                                                    | EU · US · Middle East (UAE/Saudi)                                                                         | Choose by where the first customers are. If the primary market is MENA, pick the closest available region — latency and data-residency questions will be asked by enterprise buyers | Migrating regions after launch is disruptive                                                  |
| **D-04** | **Confirm the three-interface separation and the admin hostname strategy** | Separate hostname (recommended) · path-based separation                                                   | Separate hostname                                                                                                                                                                   | Path-based separation weakens the isolation guarantee                                         |
| **D-05** | **Legal entity, invoicing jurisdiction, and tax obligations**              | —                                                                                                         | Needed before billing design is finalized, and it affects currency and tax configuration                                                                                            | Wrong invoicing setup is a legal and accounting problem, not just a code change               |

### 4.2 Blocking Phase 3 (Plans, Entitlements, Credits) — **ANSWERED 2026-09-07**

All seven are now decided. The full text of each answer is in the decision log (§5); this table is the
index. **The values are configuration, never source** — `AC-04.3` fails the build if a plan name, price,
limit or trial duration appears in application code.

| ID       | Decision needed                                 | Owner's answer                                                                                                                         |
| -------- | ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| **D-06** | Plan structure: how many tiers, and their names | **Starter · Growth · Scale · Enterprise.** "Agency" is not used as a plan name (D-62)                                                  |
| **D-07** | Price points per tier, per currency             | **Provisional:** $29 / SAR 109 · $79 / SAR 299 · $199 / SAR 749 · custom. Annual = 10 monthly payments. Review again before production |
| **D-08** | Supported currencies at launch                  | **SAR + USD**, no runtime conversion. Matches the shipped `supportedCurrencies` default                                                |
| **D-09** | Trial length and card requirement               | **14 days, no card, one per workspace, 200 trial credits.** No permanent free plan at launch                                           |
| **D-10** | Quota values per tier                           | Seats 2/8/20 · brands 1/3/10 · social accounts 3/12/40 · posts/month 100/500/2,000 · storage 5/50/250 GB · analytics 90d/12mo/24mo     |
| **D-11** | Overage policy                                  | **Hard stop on every plan. Prepaid top-ups only. No postpaid overage, no surprise invoice charges** — stricter than was recommended    |
| **D-12** | Credit expiry policy                            | Packs 12 months · promotional 3 months · plan credits roll over up to 1× allowance · FIFO by nearest expiry                            |

**Still unanswered and still blocking**: nothing in §4.2. Phase 3 implementation is unblocked on the
commercial decisions; it has not been authorised to start.

### 4.3 Blocking Phase 4 (AI Gateway) — **ANSWERED 2026-09-13**

> D-13, D-15, D-16 and D-17 were approved by the product owner on 2026-09-13. The rows below are the
> recommendations as put; the approvals, with the conditions attached to them, are in §4.3.1.

| ID       | Decision needed                     | Options                                                  | Recommendation                                                                                                                                                                                                    |
| -------- | ----------------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-13** | **Which AI providers for launch**   | One primary + one fallback · multi-provider from day one | One primary provider plus one fallback per modality. **Requirement: the provider's terms must include no-training / zero-retention for our data** — this is a hard filter, and it must be verified before Phase 4 |
| **D-14** | **Credit granularity**              | Whole credits (integer) · milli-credits (bigint)         | **Milli-credits internally, whole credits displayed.** Cheap tasks otherwise round badly, and changing this after launch means migrating the ledger                                                               |
| **D-15** | **Target gross margin on AI**       | 50% · 60% · 70%+                                         | 60–70% on AI usage, so provider price rises can be absorbed without repricing customers                                                                                                                           |
| **D-16** | **Which modalities are in the MVP** | Text only · text + image · text + image + video          | **Text + image.** Video generation is expensive, slow, and immature — a Phase 7+ candidate                                                                                                                        |
| **D-17** | **Arabic model quality bar**        | —                                                        | Requires a side-by-side evaluation of candidate models on Arabic marketing copy **before** Phase 4 routing is configured. Budget time for this; it is the single biggest quality risk in the product              |

#### 4.3.1 As approved — 2026-09-13

| ID       | Approved                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-13** | **Provider architecture approved; exact providers pending benchmark, privacy verification and owner approval.** One primary provider plus one fallback per supported modality. Vendor selection happens only after: Arabic and English quality benchmarking · current pricing comparison · privacy and data-processing review · confirmation that customer data is not used for provider training · confirmation of zero retention or an acceptable equivalent · owner approval of the final provider/model routing table. Provider names, models and routing remain configuration-driven. **No real production provider adapter is built or enabled.**                                                                                   |
| **D-15** | **Target gross margin on AI usage: 65%.** An internal commercial target, NOT a hard-coded markup. Provider cost, customer credit charge, routing and margin calculation remain configuration-driven and versioned. Prices are derived as `customer price = provider cost / (1 - target gross margin)`. Published package prices are unchanged and final per-action credit prices are **not** set here — they will be calibrated from real provider benchmarks.                                                                                                                                                                                                                                                                            |
| **D-16** | **MVP modalities: text generation and transformation, and image generation and editing.** Video generation is **excluded from the MVP** and recorded as a **Phase 7+ candidate requiring a separate cost, latency and product review**. Voice remains post-MVP. Embedding and moderation are internal plumbing rather than customer-facing generation modalities and are unaffected.                                                                                                                                                                                                                                                                                                                                                      |
| **D-17** | **No provider/model may be enabled for production customer routing until it passes a documented side-by-side Arabic marketing-content benchmark.** The benchmark must evaluate, at minimum: Arabic grammar and spelling · Modern Standard Arabic quality · Saudi/Gulf localisation where requested · Brand Voice adherence · instruction following · marketing usefulness and originality · English/Arabic consistency · safety and factual reliability · structured-output reliability · latency and cost. Defined in **`docs/AI-QUALITY-BENCHMARK.md`**. **No paid provider evaluation has been run and no winning model has been selected** — both need the owner's go-ahead.                                                          |
| **D-78** | **AI output persistence policy.** `persistOutput` defaults to `false`. The minimum operational metadata required for usage, cost, credits, idempotency, auditing and diagnostics is always retained. Raw provider prompts and raw provider responses are never persisted by default. A user-facing AI result is persisted only when the calling product feature explicitly requires it. Anything persisted is tenant-isolated and covered by a defined retention and deletion policy. Sensitive data, secrets and provider credentials are never stored in prompts, outputs, logs or audit metadata. The feature/domain that requests persistence owns the saved artifact — **the AI Gateway must not become a permanent content store.** |

**How each is enforced rather than merely recorded.** A decision that lives only in this file is one
a future change can contradict without anything noticing:

| Decision | Where it binds                                                                                                                                                                                                                                                              |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-13     | `ai.providers` carries `noTrainingGuarantee`, `dataRetentionPolicy` and `privacyReviewRef`; configuration validation **refuses to activate** a provider that has not cleared all three. The no-training check was a warning while the decision was open and is now an error |
| D-15     | `ai.credit-rules.targetGrossMarginPercent` (nullable, no invented value) plus `requiredPriceMicroMinor()`, which implements the division rather than a markup. A unit test asserts the two are not the same number                                                          |
| D-16     | The task catalogue marks `video.generate` and `voice.synthesize` as outside MVP scope, and `resolveRoute` refuses them — so an out-of-scope task cannot be served even if a rule for it were activated                                                                      |
| D-17     | `ai.models.qualityBenchmarkRef`; validation refuses status `available` without it. `beta` is exempt, because beta is the status a model sits in while it is being benchmarked                                                                                               |
| D-78     | `persistOutput` defaults false; validation refuses it without `outputRetentionDays`; `purgeExpiredOutputs()` clears expired payloads and leaves the accounting intact                                                                                                       |

### 4.4 Blocking Phase 6 (Social Publishing)

| ID       | Decision needed                                             | Options                           | Recommendation                                                                                                                                                         |
| -------- | ----------------------------------------------------------- | --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-18** | **Launch platform priority**                                | All six at once · a phased subset | **Instagram + Facebook + LinkedIn first**, then TikTok, YouTube, X. This covers most SMB and agency demand with the most stable APIs                                   |
| **D-19** | **Start platform app review and business verification now** | —                                 | **This is a lead-time item that must start during Phase 1–2, not Phase 6.** Meta and TikTok reviews can take weeks and can block launch even when the code is finished |
| **D-20** | **X (Twitter) API tier**                                    | Free · Basic · Pro                | Depends on expected posting volume; the paid tiers are a real, ongoing cost that must be priced into plans                                                             |

### 4.5 Blocking Phase 8 (Billing and Launch)

| ID       | Decision needed                                      | Options                                                                                 | Recommendation                                                                                                                                                     |
| -------- | ---------------------------------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **D-21** | **Payment provider(s)**                              | A single global provider · a global provider + a regional one for local payment methods | Start with one global provider for speed; the abstraction lets a regional provider be added when local methods (mada, KNET, local cards) matter for conversion     |
| **D-22** | **Tax handling**                                     | Provider tax engine · manual configuration                                              | Provider tax engine where available — VAT rules are not worth hand-maintaining                                                                                     |
| **D-23** | **Refund policy**                                    | No refunds · 14-day money-back · pro-rated                                              | 14-day money-back on first purchase; pro-rated at the owner's discretion afterwards                                                                                |
| **D-24** | **Launch market and go-to-market language priority** | MENA-first (Arabic-primary) · global (English-primary) · both equally                   | Affects which locale is the default, which currency leads, SEO strategy, and content investment — please decide explicitly                                         |
| **D-25** | **Legal documents**                                  | —                                                                                       | Terms, Privacy Policy, DPA, Acceptable Use, and Refund Policy need legal review before launch. Engineering provides the technical facts; the owner arranges review |

### 4.6 Cross-cutting approvals

| ID       | Decision needed                                                                                             | Recommendation                                                                                                                                             |
| -------- | ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-26** | **Approve the MVP vertical slice scope** as defined in `docs/MVP-ACCEPTANCE-CRITERIA.md`                    | Approve as written; it is deliberately narrow so the hard invariants (isolation, credits, configuration, audit) are proven before feature breadth is added |
| **D-27** | **Approve mandatory 2FA for platform roles from Phase 2** (stronger than the stated "future 2FA support")   | Approve — admin compromise is the highest-severity risk in the system                                                                                      |
| **D-28** | **Approve the prohibition on true impersonation** in support mode                                           | Approve — support gets read-only, audited, time-boxed access instead                                                                                       |
| **D-29** | **Approve the isolation-test CI gate** (a new tenant-owned model without an isolation test fails the build) | Approve — this is what makes A-05 enforceable over time rather than aspirational                                                                           |
| **D-30** | **Confirm no real credentials are used before Phase 6** and that sandbox/test credentials are used first    | Approve                                                                                                                                                    |

### 4.7 Blocking the first production release of the Asset Library

**The library is complete and tested end to end against the development stores. It refuses to run in production until these two are answered**, because the alternative is accepting a customer's file and quietly losing it, or reporting it as scanned when nothing scanned it.

| ID        | Decision needed                                                                     | Options                                                                                                     | Recommendation                                                                                                                                                                                             |
| --------- | ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-110** | **Object-storage provider and region for customer files** (F-77)                    | Any S3-compatible service · a managed provider bundled with the hosting choice in D-02 · self-hosted        | **Decide alongside D-02 and D-03**, so files and the database share a region. The adapter goes behind the existing `ObjectStore` interface; nothing outside `packages/storage` changes.                    |
| **D-111** | **Virus-scanning engine, and whether customer bytes may leave the platform** (F-78) | Hosted scanning API (bytes leave) · self-managed engine in the worker network (bytes stay, you run updates) | **Self-managed, if the data region in D-03 is a privacy commitment.** A hosted API is less operational work and a larger privacy surface; that trade is the owner's to make, not an implementation detail. |

---

## 5. Decisions Made (log)

Record decisions here as they are taken, so the project's history stays in one place.

| Date       | ID         | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Decided by                                      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-01 | —          | Phase 0 architecture documented and submitted for review                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Architecture                                    | Awaiting owner review of §4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-09-01 | —          | **Phase 0 approved and merged.** Phase 1 authorised to begin                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Product Owner                                   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-09-01 | **D-01**   | Technology stack **approved as proposed**: Next.js + TypeScript, Node.js/TypeScript, PostgreSQL + Prisma, Redis + BullMQ, S3-compatible storage, Zod, Auth.js, Vitest + Playwright, OpenTelemetry                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Product Owner                                   | Implemented in Phase 1. Prisma pinned to **7.10.0** (stable) rather than the 8.x release candidate, and TypeScript to **5.9.3** rather than the newly released 7.x — foundations should not sit on an RC or a brand-new major.                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-09-01 | **D-02**   | Hosting: **provider-agnostic architecture**. Intended initial target is Render (web, API, workers, PostgreSQL, Redis), Cloudflare R2 (media), Cloudflare (DNS, CDN, WAF, rate limiting). Nothing deployed or purchased in Phase 1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Product Owner                                   | All infrastructure is environment configuration (`packages/shared/src/env.ts`). No provider name or SDK appears in application code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-09-01 | **D-03**   | Primary market **Saudi Arabia / GCC**. Design for Middle East data residency; use the closest compliant region at deployment. Keep the region portable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Product Owner                                   | `DATA_REGION` is configuration. Defaults reflect the market: timezone `Asia/Riyadh`, currency `SAR`, country `SA`, default locale **Arabic**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-09-01 | **D-04**   | Hostnames approved: `brandspace.cc` / `www` (public), `app.brandspace.cc` (customer), `admin.brandspace.cc` (Platform Admin). Session realms remain separated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Product Owner                                   | Implemented in `packages/auth/src/realms.ts`: different cookie names, token audiences, signing-key sources, TTLs and SameSite per realm. Asserted by `tests/unit/realms.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-09-01 | **D-05**   | Launch jurisdiction **Saudi Arabia**. Legal entity, tax registration and invoicing details **remain TBD**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Product Owner                                   | Does not block Phase 1. **Blocks Phase 8**: production billing, taxation, invoicing, accepting payments. Still open in §4.5.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-09-01 | **D-14**   | AI usage accounting uses **milli-credits internally**, whole credits displayed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Product Owner                                   | Not yet implemented (Phase 3/4). Recorded in `docs/DATABASE.md` §1 so ledger columns are sized correctly from the first credit migration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-09-01 | **D-27**   | **Mandatory 2FA for all Platform Admin roles** — approved.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Product Owner                                   | Enforced in Phase 1 at the one place that exists: `asPlatform()` refuses an actor without `mfaVerified`, and `PLATFORM_REALM.requiresMfa = true`. Login enforcement lands with auth flows in Phase 2.                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-09-01 | **D-28**   | **True impersonation prohibited.** Support Mode must be audited, time-limited, read-only by default, and require an explicit reason.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Product Owner                                   | `SupportModeSession` carries `reason`, `expiresAt`, `writeEnabled` (default false). Rows are visible to the workspace they target, so a customer can see that support accessed their data.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-09-01 | **D-29**   | **CI must fail when a tenant-owned model lacks isolation coverage** — approved.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Product Owner                                   | Implemented as `scripts/isolation-gate.ts` plus a dedicated CI job. Verified to fail correctly by `tests/unit/isolation-gate.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-09-01 | —          | Meta and TikTok app review recorded as an **early owner action**; social integrations not implemented in Phase 1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Product Owner                                   | Still open — §4.4 D-19. Multi-week lead time; can block launch with finished code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-09-01 | —          | Arabic AI model quality evaluation to be completed **before** production AI routing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Product Owner                                   | Still open — §4.3 D-17. Gates Phase 4 routing configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-09-01 | **F-01**   | **Platform operations get a dedicated database role and connection pool.** Cross-tenant visibility is granted by role-targeted RLS policies (`platform_access TO brandspace_platform`), not by an application-settable session variable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Product Owner                                   | The `app.is_platform_mode()` GUC is dropped. `brandspace_app` cannot reach platform visibility by any SQL it can execute: no policy names it, and it is not a member of the platform role so `SET ROLE` fails. Neither role has `BYPASSRLS`, so the platform policy is still evaluated normally and `WITH CHECK` applies.                                                                                                                                                                                                                                                                                            |
| 2026-09-01 | **F-01a**  | Role membership is owned by `scripts/sql/setup-database-roles.sql`, not by migrations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Architecture                                    | Revoking a role membership needs ADMIN option on that role; granting the migrator that power would make it a privilege-escalation path in its own right. The migration therefore ASSERTS the separation and refuses to deploy without it.                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-09-01 | **F-04**   | **Playwright E2E with Chromium only** for now.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Architecture                                    | Phase 1 has three near-identical scaffold pages; three engines would triple CI time to re-assert the same markup. More projects when there is behaviour that could plausibly differ between engines.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-09-01 | **F-04a**  | Accessibility CI fails on **serious and critical** axe violations; minor and moderate are reported but non-blocking.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Architecture                                    | A build that fails on cosmetic findings gets muted, and a muted gate protects nobody. axe is a smoke test — it catches roughly a third of real accessibility problems and does not replace manual audit.                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-09-01 | **A11Y-1** | **Brand blue is not usable as text.** `#00ADEE` scores 2.56:1 on white; the original `brandBlueInk` white-on-`brandBlue` button pairing also scored 2.56:1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Architecture                                    | Found by the new axe suite. Added `brandBlueText` / `brandBlueSurface` (`#00658A`, 6.50:1) alongside the identity blue, mirroring the existing yellow treatment. Every documented pairing is now asserted in `tests/unit/contrast.test.ts`, so a token change that breaks contrast fails a fast unit test. The approved brand colours are unchanged — only their permitted uses are now explicit.                                                                                                                                                                                                                    |
| 2026-09-01 | —          | **Prisma CLI configuration resolves database URLs only for commands that connect.** `generate`, `validate` and `format` are offline and require no environment; `migrate`, `db`, `studio` and the seed fail closed with an actionable, redacted error.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Product Owner (Option A)                        | Fixes the defect that took three CI jobs down on PR #2. `datasource` is omitted entirely for offline commands, which Prisma's own typing permits ("required for migration / introspection commands"). Regression-tested in `tests/unit/prisma-config.test.ts` with a scrubbed environment.                                                                                                                                                                                                                                                                                                                           |
| 2026-09-01 | **D-30**   | **Configuration is edited as JSON in the Control Center for Phase 2A.** Per-domain form editors arrive with the domains that need them (plans, routing, credit rules).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Architecture                                    | A console that can create and activate drafts but not change a value is not a configuration console. The JSON editor is honest about what it is, is validated in two stages before activation, and carries the same optimistic-concurrency guard a form editor would.                                                                                                                                                                                                                                                                                                                                                |
| 2026-09-01 | **D-31**   | **`plans` and `ai.credit-rules` require dual control**: the activator may not be the author.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Architecture                                    | These two domains are the ones where a mistake charges customers money. Every other domain activates with a single operator plus an explicit acknowledgement when the impact preview reports high-impact changes.                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-09-01 | **D-32**   | **The Secret Service has no reveal path, by construction.** `resolveSecret()` is the only decrypt entry point and it is server-side; nothing returns a plaintext value to a caller that could render it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Architecture                                    | A "show secret" feature is the single most common way a vault becomes a liability. Operators get a masked hint (last four characters), a keyed fingerprint for "is this the key I meant?", and rotation. Asserted end to end: the plaintext appears in no response body.                                                                                                                                                                                                                                                                                                                                             |
| 2026-09-01 | **D-33**   | **`platform_user` is platform-owned data**, not a global catalogue.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Architecture                                    | Corrects a real exposure found while writing the Phase 2A tests — see F-10. The isolation gate now requires every model to be classified deliberately, so "nobody thought about it" is a build failure rather than a default.                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-09-01 | **D-34**   | **The end-to-end suite runs against a real database with a throwaway admin account** created seconds earlier by `pnpm e2e:seed`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Architecture                                    | The Control Center is session-gated and database-backed; a suite that only exercised the signed-out state would prove nothing about the product. No real credential is involved: the password, TOTP seed and recovery codes are generated per run and written to a git-ignored file.                                                                                                                                                                                                                                                                                                                                 |
| 2026-09-02 | **D-35**   | **Configuration and secrets get their own least-privilege permissions**, split read / manage / activate. `platform.workspace.read` no longer authorises any write.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Independent security review                     | Closes R-02. Viewing a customer's workspace and rotating a production API key were the same authority; support, billing and operations roles all held it.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-09-02 | **D-36**   | **The domain services are the authorization boundary**, not the pages or server actions. `ConfigActor` and `SecretActor` now carry `permissionKeys` and every method asserts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Independent security review                     | A server action is a public HTTP endpoint. A check that lives only in the UI layer can be bypassed by calling the service directly, which the review demonstrated.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-09-02 | **D-37**   | **Only fixed error CODES cross to the browser.** Server actions emit a code from a closed allowlist plus an opaque correlation id; the real error is logged once, redacted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Independent security review                     | Closes R-05. `AppError.toPublicJSON()` already omitted `message` for this reason; the redirect path did not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-09-02 | **D-38**   | **The seed has no default password.** `SEED_PLATFORM_PASSWORD` absent -> the Platform Owner is created with no password and cannot sign in. Present but weak or placeholder -> the seed fails.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Independent security review                     | Closes R-04. A committed password is a known credential for every database the seed is ever pointed at.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-09-02 | **D-39**   | **A recovery code is consumed by a conditional UPDATE**, not read-then-write. The database comparison replaces `timingSafeEqual`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Independent security review                     | Closes R-03. A deliberate trade: the compared value is a SHA-256 hash of a high-entropy code, so a timing channel reveals nothing usable, while losing atomicity handed an attacker a second use of a code the owner believed was spent.                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-09-02 | **D-40**   | **Phase 2B pulls the entitlement MACHINERY forward from Phase 3, and no commercial data with it.** The precedence engine, workspace overrides and the credit ledger ship now; plan names, prices and allowances stay configuration and remain unset.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Product owner (Phase 2B brief)                  | CLAUDE.md §2.7 requires the owner's approval for pulling work forward, recorded here. D-06…D-12 are still unanswered, so nothing invents a tier, a price or a quota. Plan assignment lives on `Workspace.planKey` until `Subscription` arrives with billing.                                                                                                                                                                                                                                                                                                                                                         |
| 2026-09-02 | **D-41**   | **No email vendor is chosen.** Sending is an interface; the default implementation writes to an auditable `email_message` outbox. An action link is delivered, never persisted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Product owner (Phase 2B brief)                  | The brief forbids selecting a vendor without approval. The outbox is deterministic, offline and inspectable, which is what tests and operators need before a vendor exists.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-09-02 | **D-42**   | **The application palette for the Control Center and the customer dashboard is purple `#7935FE` with yellow `#FFDD15` as the accent, Inter for Latin and Cairo for Arabic.** The public marketing site keeps the identity blue `#00ADEE`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Product owner (Phase 2B brief)                  | Recorded because it amends CLAUDE.md §4, which names blue as the primary. Purple scores 5.60:1 on white, so unlike the blue it is legible BOTH as text and as a filled surface — asserted in tests/unit/contrast.test.ts.                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-09-02 | **D-43**   | **`SupportModeSession` stays TENANT-owned.** A platform actor starts it; the customer must be able to see it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Phase 2B implementation                         | docs/SECURITY.md §8 requires the workspace's own Activity Log to show that support accessed it, with reason and duration. A platform-owned row would be invisible to the customer — the opposite of the guarantee. No contradiction; no reclassification.                                                                                                                                                                                                                                                                                                                                                            |
| 2026-09-02 | **D-44**   | **The customer application reads entitlement configuration from a PROJECTION, never from `configuration_version`.** The Configuration Service writes `entitlement_catalogue_snapshot` for the three customer-visible domains (`entitlements`, `plans`, `feature-flags`) in the same transaction that activates a version.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Phase 2B implementation                         | The tenant role has no privilege on `configuration_version`, and granting it one would expose `integrations.*` and `operations` payloads to every signed-in customer. A CHECK constraint pins the allowed domains, so a fourth domain cannot be projected by accident. `SECURITY DEFINER` was rejected: under `FORCE ROW LEVEL SECURITY` a function owned by the migrator is itself subject to policy, and no policy names the migrator, so it returns nothing.                                                                                                                                                      |
| 2026-09-03 | **D-45**   | **`listWorkspaces` REFUSES a token that matches no live session** instead of returning an empty list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Phase 2B implementation                         | An empty list is indistinguishable from a genuine member-of-nothing. That ambiguity hid a real defect for a full test cycle — see F-20. An unknown token now raises `UNAUTHENTICATED`, and the dashboard redirects to sign-in rather than telling a signed-in member they belong to nothing.                                                                                                                                                                                                                                                                                                                         |
| 2026-09-03 | **D-46**   | **Invitation redemption gets ONE `SELECT`-only, pending-only, token-keyed RLS policy on the tenant role**, and performs every write inside the workspace context instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Phase 2B implementation                         | The holder of a link is not a member yet, so no workspace context exists to bind. Relaxing `tenant_isolation` would have let the tenant role enumerate every pending recipient in every workspace; handing the customer application the platform credential is forbidden by F-07; `SECURITY DEFINER` cannot work under `FORCE ROW LEVEL SECURITY`. Asserted in `tests/isolation/rls-raw-sql.test.ts` and exercised on the tenant role in `tests/isolation/invitations.test.ts`.                                                                                                                                      |
| 2026-09-03 | **D-47**   | **The outbox is written under the context the message belongs to.** A workspace-scoped message is written inside that workspace's transaction; a workspace-LESS message (password reset) is written with no context, permitted by a write-only widening of `email_message`'s `WITH CHECK`. Reads stay workspace-scoped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Phase 2B implementation                         | Mirrors the audit-event fix in migration 20260902230000. A reset must answer identically whether or not an account exists, so it cannot resolve a workspace without becoming an existence oracle — and `NULL = NULL` is not true, so every such row was refused. Both sends are wrapped so a mail failure cannot change the response, which meant the rows were being dropped SILENTLY.                                                                                                                                                                                                                              |
| 2026-09-03 | **D-48**   | **`visuallyHiddenStyle()` and `scrollContainerStyle()` live in `@brandspace/ui`**, typed structurally so the package stays framework-free.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Phase 2B implementation                         | A hand-rolled screen-reader-only label inside a scrolling table extended the DOCUMENT's scroll width, because an absolutely positioned element escapes the clip of a scrolling ancestor that is not its containing block. The fix is two declarations that must travel together, so they are one helper pair rather than four copies. Pinned by `tests/unit/a11y-style.test.ts`.                                                                                                                                                                                                                                     |
| 2026-09-03 | **D-49**   | **Phase 2C is a UI FOUNDATION inserted between Phase 2B and Phase 3.** It changes no database ownership, no RLS policy, no permission and no commercial assumption, and pulls no functional feature forward. The roadmap is unchanged: Phase 3 still follows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Product owner (Phase 2C-A brief)                | Recorded because inserting a phase is itself a roadmap change under CLAUDE.md §2.7, even when it adds no product capability. No migration was created; the brief said to stop and explain if one seemed unavoidable, and none was.                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-09-03 | **D-50**   | **The application ground is WHITE**, amending the `#FAFAFA` recorded in D-42. Separation between a card and the page comes from a subtle border plus one restrained shadow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Product owner (Phase 2C-A brief)                | The brief names white as the main background and asks for "clean white surfaces, subtle neutral borders". `appBackground` remains its own token so the decision is one edit, not a search for `#FFFFFF`; a unit test asserts it equals `surface` so a silent divergence fails the build.                                                                                                                                                                                                                                                                                                                             |
| 2026-09-03 | **D-51**   | **One icon family, drawn in this repository**, rather than an icon dependency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Phase 2C-A implementation                       | The brief permits a library "only if necessary". These surfaces need about two dozen glyphs; a dependency would bring thousands, a bundle cost, a supply-chain surface and a second visual language whose stroke weight nobody controls. Drawn to one specification: 24×24, 1.75 stroke, round caps, `currentColor`, `aria-hidden` by default.                                                                                                                                                                                                                                                                       |
| 2026-09-03 | **D-52**   | **Webfonts are a RUNTIME option, never a build dependency.** Cairo and Inter load from a stylesheet link only when `BRANDSPACE_WEBFONTS=google`; every stack ends in system faces.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Phase 2C-A implementation                       | `next/font/google` downloads faces at build time and fails the build when it cannot reach the host — F-06 in a new costume: verification that passes only under ambient conditions. Tests and CI stay hermetic, and both scripts render correctly offline.                                                                                                                                                                                                                                                                                                                                                           |
| 2026-09-03 | **D-53**   | **The design showcase is gated on `APP_ENV`, not `NODE_ENV`,** and additionally on an explicit opt-in flag.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Phase 2C-A implementation                       | `next start` always sets `NODE_ENV=production`, so a gate keyed on it is a deletion rather than a gate — the route could never be served from a production build, including in the end-to-end suite, which is how the first draft was caught. `APP_ENV` is the deployment environment the repository already uses.                                                                                                                                                                                                                                                                                                   |
| 2026-09-04 | **D-54**   | **The visual hierarchy is INVERTED: surface, space, type, shape and shadow separate things — a border is a hairline of last resort.** This amends D-50, which said separation comes from "a subtle border plus one restrained shadow".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Product owner (Phase 2C-A visual revision)      | D-50 put a visible border on a white card standing on a white ground, which made the border the only thing doing the work and made the product read as a traditional outlined admin console — the owner's stated objection. The canvas stays white; supporting surfaces (lavender, warm grey, off-white) now carry structure, controls are filled rather than outlined, and `hairline` is deliberately invisible at rest.                                                                                                                                                                                            |
| 2026-09-04 | **D-55**   | **A borderless control satisfies WCAG 1.4.11 through an always-visible label, a 5.6:1 focus ring and a 3:1 tone border — and full boundaries are RESTORED under `prefers-contrast: more` and `forced-colors: active`.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Phase 2C-A visual revision                      | A soft `#F4F4F7` fill on white is 1.06:1, and no soft fill can reach 3:1. Rather than claim otherwise, the system makes the control identifiable without its edge and gives the people who need edges real ones. This is the escape hatch the brief's own §4 sanctions ("a very soft neutral border may be used where necessary for accessibility"), used deliberately rather than as a default.                                                                                                                                                                                                                     |
| 2026-09-04 | **D-56**   | **The AI Copilot is CONTEXTUAL, not a floating chat window.** It names the surface and subject it is docked to, and each surface offers only the actions it can serve (`SURFACE_ACTIONS`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Product owner (Phase 2C-A visual revision)      | The brief asked for a Copilot integrated into the calendar, library, composer and Studio rather than a generic chatbot. A design canvas that offers to suggest a posting time is the same defect as a button that does nothing. The approval gate and the inert composer (CLAUDE.md §2.5) are unchanged; a proposed action now additionally shows a before/after preview of exactly what would change.                                                                                                                                                                                                               |
| 2026-09-04 | **D-57**   | **Preview media is DETERMINISTIC BRAND ARTWORK, never an empty grey box and never a fetched image.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Phase 2C-A visual revision                      | The brief forbids grey placeholder boxes as the primary visual. `AbstractMedia` composes a gradient and a few shapes from one of six palettes derived from the brand purple and yellow, selected by a seed: it fetches nothing, so no build or test depends on a network, and it renders identically on every run, so a screenshot means the same thing twice.                                                                                                                                                                                                                                                       |
| 2026-09-04 | **D-58**   | **The read-only workspace role is labelled "Viewer (read-only)". The stored RBAC key `client_viewer` and its single `workspace.read` grant are UNCHANGED.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Product owner (Phase 2C-A visual revision, §18) | "Client Viewer" framed the narrowest role as an outside client of an agency, which is one customer shape among six and not the product's own language. The key is written into membership rows and asserted by the RBAC and isolation suites, so renaming it would be a data migration wearing a copy change. A unit test now fails if a future edit renames the key or widens the role while relabelling it. The seeded sample tenant is likewise a brand ("Sample Brand"), not "Acme Agency".                                                                                                                      |
| 2026-09-06 | **D-59**   | **The approved demo is the visual authority, and it is vendored into the repository.** `docs/visual-reference/{index.html,styles.css,app.js}` are the owner-approved files, kept verbatim and excluded from ESLint and Prettier. The direction they define rests on two decisions that supersede D-49 and tighten D-54: (a) the ground is no longer white — a grey `#F2F2F2` canvas carries three enormous blurred brand-coloured orbs, and a near-white shell FLOATS on it, which is what lets every card drop its resting border entirely; (b) there are TWO primary treatments, and the ratio between them is part of the design — near-black `ink` carries almost every action, and the brand purple is reserved for the single "create" entry point. Full-saturation brand colour appears in exactly one place in the product: the ambient orbs. | Phase 2C-A demo alignment                       | Four deviations from the reference are deliberate and recorded in `docs/visual-reference/README.md`: its `--muted` is 4.39:1 on its own ground and its focus ring is 1.67:1, so neither was copied; a phone gets a real drawer rather than the reference's permanent 68px icon rail (§6); collapsed tooltips fire on focus as well as hover and carry an accessible name; and its 8–9px type and 1.89:1 out-of-month grey are not reproduced at all.                                                                                                                                                                 |
| 2026-09-06 | **D-60**   | **The FULL DEMO supersedes the three-file reference as the visual authority.** `docs/visual-reference/full-demo/` is a byte-for-byte snapshot of `Mohamed-Omaar/Brandspace-Landing-page` at commit `10765e8cf4f5b89c91b144863330459611248b16` — nine `demo/` files plus `brand-brain-preview/index.html`, with SHA-256 checksums recorded in that directory's README. The earlier reference moves to `superseded-2026-09-05/` and loses authority wherever the two disagree. `brand-brain-preview/index.html` is recorded as the FUTURE authority for Phase 5's Brand Brain screens; nothing from it is implemented. The upstream repository is read-only for this project and was never written to.                                                                                                                                                  | Product owner (complete visual fidelity pass)   | The target is stated as pixel-level reproduction of the demo for the screens it covers, not a shared design language. That standard is only meaningful against files that cannot drift, which is why the snapshot is pinned to a commit and checksummed. The four deviations D-59 recorded were re-derived against the full demo's own values rather than carried over; the current list — eleven rows, each an accessibility, security, real-data or phase-boundary reason — is in `docs/visual-reference/README.md`.                                                                                               |
| 2026-09-07 | **D-61**   | **Purple `#7935FE` is the brand colour EVERYWHERE, including the public marketing site. Yellow `#FFDD15` is the accent. Ink `#111114` and white carry the interface. `#00ADEE` is a RETIRED legacy identity colour and must not appear in new product or marketing UI.** This closes the carve-out D-42 left open, in which the marketing site kept the blue. The vendored demo remains the visual authority (D-60).                                                                                                                                                                                                                                                                                                                                                                                                                                  | Product owner (Phase 3 documentation gate)      | Three facts forced the question. The demo D-60 makes authoritative contains **zero** occurrences of `#00ADEE` across all ten files, and its own `.public-*` marketing screens use purple, yellow and white. `brandBlue` is defined in `packages/ui/src/tokens.ts` and consumed by **no application file anywhere in the repository**. And four documents still named blue as primary, all of them written before D-42. Rather than let a decision and its documentation disagree, the owner chose one identity. The token stays defined so A-20 and A11Y-1 remain readable; a unit test asserts nothing consumes it. |
| 2026-09-07 | **D-62**   | **The MVP is for businesses, founders, in-house brand teams and multi-brand companies. It is NOT an agency operating system.** No Agency plan, no Client Viewer feature exposed or promoted, no agency portals, client hand-off workflows, or agency-specific language. Agency capability is possible future expansion, documented outside the MVP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Product owner (Phase 3 documentation gate)      | Narrows A-22, which listed agencies among the supported customer types. Building for agencies means client portals, white-labelling and hand-off workflows — U-04 already deferred white-labelling for exactly this reason. The stored RBAC key `client_viewer` is UNCHANGED: it is written into membership rows and asserted by the isolation suites, so removing it is a data migration, not a copy change (the same reasoning as D-58). It is simply not offered as a plan feature.                                                                                                                               |
| 2026-09-07 | **D-06**   | **Four plans: Starter, Growth, Scale, Enterprise.** Three public tiers plus a negotiated tier. "Agency" is not used as a plan name anywhere.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Product owner (Phase 3 documentation gate)      | Answers the D-06 question. "Scale" replaces the proposed "Agency" because of D-62 — the tier is for larger and multi-brand BUSINESSES, and naming it Agency would advertise a positioning the product does not have.                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-09-07 | **D-07**   | **Provisional launch pricing.** Starter $29 / SAR 109 per month · Growth $79 / SAR 299 · Scale $199 / SAR 749 · Enterprise custom. Annual = **10 monthly payments** (two months free).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Product owner (Phase 3 documentation gate)      | Explicitly PROVISIONAL commercial configuration. It must stay configuration — `AC-04.3` fails the build if any price reaches source — and must be reviewed again before production launch. SAR prices are set explicitly, never FX-converted at runtime.                                                                                                                                                                                                                                                                                                                                                             |
| 2026-09-07 | **D-08**   | **SAR and USD at launch. No runtime currency conversion.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Product owner (Phase 3 documentation gate)      | Matches the shipped `platform.settings` default (`supportedCurrencies: ['SAR','USD']`), so no configuration change is required. AED is the natural fast follow — it is pegged, so its price table is mechanical. EUR is deliberately deferred: it brings VAT-MOSS obligations that touch D-22.                                                                                                                                                                                                                                                                                                                       |
| 2026-09-07 | **D-09**   | **Trial: 14 days, no card required, one per workspace, 200 trial credits. No permanent free plan at launch.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Product owner (Phase 3 documentation gate)      | Confirms the standing recommendation and U-05. Trial reminders at 7/3/1 days already exist in `docs/BILLING-AND-CREDITS.md` §3.1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-09-07 | **D-10**   | **Quotas per plan** — seats 2/8/20, brands 1/3/10, social accounts 3/12/40, scheduled posts per month 100/500/2,000, storage 5/50/250 GB, analytics retention 90 days / 12 months / 24 months, across Starter / Growth / Scale. Enterprise is negotiated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Product owner (Phase 3 documentation gate)      | These are exactly the six dimensions `AC-04.2` requires a plan to carry, so the acceptance criterion is satisfiable without inventing a seventh. Values are configuration and remain editable.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-09-07 | **D-11**   | **Hard stop at zero credits on every plan, for the MVP. Prepaid top-ups only. NO postpaid overage and no surprise invoice charges.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Product owner (Phase 3 documentation gate)      | Stricter than the standing recommendation, which allowed opt-in overage on higher tiers. The owner removed it: a bill a customer did not authorise is the wrong first impression, and hard-stop-plus-top-up is simpler to build, explain and support. Postpaid overage remains a possible post-launch addition.                                                                                                                                                                                                                                                                                                      |
| 2026-09-07 | **D-12**   | **Credit expiry and rollover.** Purchased packs expire after 12 months · promotional credits after 3 months · monthly plan credits roll over up to **one** monthly allowance · consumption is **FIFO by nearest expiry**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Product owner (Phase 3 documentation gate)      | Confirms the standing recommendation and answers U-02 with the capped option. FIFO-by-expiry is already implemented in the credit service, so this decision matches shipped behaviour rather than requiring a change.                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-09-07 | **D-63**   | **Brand Brain is the intelligence and memory layer for the entire workspace, not a premium add-on.** Every paid plan receives the real Brand Brain, a FUNCTIONAL Copilot, and AI Strategy. Plans differ by brands, credits, storage, seats, analytics depth and retention, automation capacity, and governance — never by removing the intelligence layer. Starter's Copilot is functional, governed by credits and usage limits, and is NOT read-only.                                                                                                                                                                                                                                                                                                                                                                                               | Product owner (Phase 3 documentation gate)      | Reverses the proposed matrix, which had gated AI Strategy behind Growth and made Starter's Copilot read-only. The owner's reasoning is that Brand Brain is the product's differentiator: a plan that removes it is not a cheaper BrandSpace, it is a different and worse product. Credits already meter usage, so a limit does the work that feature-gating was doing, without hollowing out the low tier. The Phase 5 schedule for the BACKEND is unchanged.                                                                                                                                                        |
| 2026-09-07 | **D-64**   | **The Brand Brain loop is a documented product principle**: Brand Brain → AI Strategy → Content Generation → Calendar/Publishing → Analytics → Learnings → Brand Brain. Relevant AI tasks retrieve from Brand Brain before generation, and meaningful approved outputs, strategies, campaigns, content decisions and performance learnings can feed back into it. Its future memory architecture separates **Canonical Brand Knowledge**, **Strategy Memory**, **Content Memory** and **Performance/Learning Memory**.                                                                                                                                                                                                                                                                                                                                | Product owner (Phase 3 documentation gate)      | Recorded now so Phases 4–7 are built against it rather than retrofitted to it. The write-back direction is the part that has to be designed early: an AI Gateway that only reads is a different system from one that also writes learnings back, and the difference shows up in the schema. Specification only — no Phase 5 implementation.                                                                                                                                                                                                                                                                          |
| 2026-09-07 | **D-65**   | **Brand Brain write-back requires provenance, evidence, confidence, approval state, versioning and reproducibility — and a human-entered rule always outranks an inferred learning.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Product owner (Phase 3 documentation gate)      | The safeguard that makes D-64's loop safe. Without it a feedback loop compounds its own errors: a weak inference becomes grounding for the next generation, which becomes evidence for the next inference. Precedence is not a preference — it is what stops the system overwriting what a customer explicitly told it.                                                                                                                                                                                                                                                                                              |

| 2026-09-07 | **D-66** | **Plan quotas live on the plan and are PROJECTED into the entitlement catalogue.** A quota written on a plan appears as a plan entitlement under a canonical key (`limit.seats`, `limit.brands`, …); an explicitly declared entitlement for the same pair still wins. | Phase 3 implementation | Two places to write a seat limit is two places for it to disagree, and the one the engine reads would win silently. The KEYS are code, exactly as `entitlements.can(ws, 'ai.image_generation')` is; every NUMBER stays configuration, which is what AC-04.3 actually forbids in source. |
| 2026-09-07 | **D-67** | **Credit buckets are a projection of the ledger, not a second source of truth.** The GRANT row carries `expiresAt` and every CHARGE row carries `sourceGrantId`, so a bucket's remaining balance is reconstructible by replay; `remaining` and `reserved` are tracked separately so an open reservation never makes a bucket disagree with the ledger. | Phase 3 implementation | The same pattern the wallet balance already uses, and covered by the same reconciliation. Computing FIFO by aggregating the whole ledger per request would be correct and unusable; a parallel truth would be usable and wrong. |
| 2026-09-07 | **D-68** | **`WorkspaceSubscription` ships now, with no payment linkage.** Plan, pinned price and its source configuration version, billing interval, cycle boundaries, trial dates, scheduled plan change. No provider reference, no invoice, no payment state. | Phase 3 implementation | AC-04.7 (a reprice must not reprice existing customers) and AC-04.11 (one trial per workspace, ever) cannot hold without a record that pins the agreed terms — a system that reads the price out of the live catalogue at render time cannot have that property. Payment collection stays Phase 7, which extends this record rather than replacing it. |
| 2026-09-07 | **D-69** | **The entitlement catalogue gets its own five-second cache, separate from the configuration cache's thirty.** Activation invalidates it directly. | Phase 3 implementation | `docs/ADMIN-CONTROL-CENTER.md` §5.5 puts kill-switch containment "within the cache TTL (seconds)", and thirty seconds is not seconds. The short TTL bounds how stale another process can be; direct invalidation is the fast path for the one that made the change. Neither replaces the cross-instance invalidation F-12 still records. |
| 2026-09-07 | **D-70** | **`ENTITLEMENT_REQUIRED`, `QUOTA_EXCEEDED` and `INSUFFICIENT_CREDITS` are public error codes.** Only the code travels; the message, which describes internal state, never does. | Phase 3 implementation | Each names the kind of wall the customer hit — not the plan, the limit or the price behind it. Hiding them behind `INTERNAL` made a wall the customer could clear look like a platform fault, and left the dashboard with no way to offer the top-up or upgrade path D-11 requires. |

---

### Phase 4 decisions taken during implementation

These were made while building the gateway, are recorded so they are visible, and are open to the owner
reversing any of them.

| ID       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-71** | **Money in the AI path is counted in micro-minor units and credits in milli-credits, both as integers; the workspace multiplier is in basis points.** No floating-point number appears in any money or credit calculation. A minor unit cannot hold a token price (F-64) and a float multiplier of 1.1 is not 1.1.                                                                                                                                                                                                                                         |
| **D-72** | **Margin is reported as UNKNOWN, never as a number, until the owner prices a credit.** `ai.credit-rules.creditValueMicroMinor` defaults to null and the margin floor does not run while it is. Reporting a healthy margin for an unpriced platform would be worse than reporting nothing. **Pricing a credit remains an owner decision (D-15/D-16).**                                                                                                                                                                                                      |
| **D-73** | **The task catalogue is code; everything about how a task is served is configuration.** A task key is the name application code calls the gateway by — the same status a feature key has — and its modality is intrinsic. Which model serves it, in what order, with what timeout, temperature, retry policy, ceiling and credit cost is configuration and appears nowhere in source.                                                                                                                                                                      |
| **D-74** | **Retryability and fallback eligibility are derived from the failure taxonomy, and `retryOn` is not configurable.** Making `content_filtered` retryable from a config screen would turn a moderation refusal into a paid retry loop. `maxAttempts` is capped at 5 for the same reason.                                                                                                                                                                                                                                                                     |
| **D-75** | **Input moderation fails open.** If the moderation model is unreachable the request proceeds; a model that answers "flagged" always blocks. A moderation outage that silently blocked every customer's work would be a far larger incident than the content it exists to catch, and it is visible through the same failure metrics as any other provider call. This is the only place in the gateway where an error is not fatal.                                                                                                                          |
| **D-76** | **The Admin AI screens show accounting, never customer content** — not even for a routing rule that opted into persisting output. Reading a customer's generated content is a Support Mode decision with its own time box and audit trail, not a side effect of opening an operations page.                                                                                                                                                                                                                                                                |
| **D-78** | **AI output persistence policy** (owner-approved 2026-09-13). `persistOutput` defaults to false; operational metadata is always retained; raw prompts and raw provider responses are never persisted by default; a user-facing result is persisted only when the calling feature requires it; anything persisted is tenant-isolated and has a defined retention and deletion policy; secrets never appear in prompts, outputs, logs or audit metadata; the requesting feature owns the artifact and the gateway must not become a permanent content store. |
| **D-77** | **`platform.ai.usage.read` is its own permission** and does not ride on `platform.workspace.read`. AI usage is a per-workspace financial record; letting "View any workspace" imply it is exactly how the configuration screens once became reachable by every admin-capable role (R-02). Granted to the Platform Owner, Platform Admin, Billing Manager and Operations Viewer; withheld from the Support Agent.                                                                                                                                           |

### Phase 5A implementation decisions (D-79 … D-84)

Taken during implementation, all reversible, each recorded because a future change that contradicts
one should have to argue with it rather than discover it.

| ID       | Decision                                                                                                                                                                   | Reasoning                                                                                                                                                                                                                                                                                                                                                                 |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-79** | **Per-area completion requirements are CODE, not configuration.** Upload rules, retention windows and freshness intervals are configuration; what "complete" MEANS is not. | A plan- or tenant-tunable definition would make the same badge mean different things to different customers, and a customer comparing their screen to the documentation would be right to call it a lie.                                                                                                                                                                  |
| **D-80** | **LEARNINGS does not count toward completion.** It is reported, and never held against the brand.                                                                          | It is written back by the system from performance evidence (D-64). Counting it would leave a customer who did everything asked of them permanently short of 100% through no fault of their own.                                                                                                                                                                           |
| **D-81** | **An accepted candidate lands as `DOCUMENT`, not `AI_INFERRED`.**                                                                                                          | A candidate a person read, judged and accepted is no longer an inference — they have taken responsibility for it. Had it stayed AI_INFERRED, `mayOverwrite` would afterwards refuse edits from the very people who approved it, and the review queue would produce knowledge nobody could maintain.                                                                       |
| **D-82** | **A chat refusal makes no gateway call and costs nothing.**                                                                                                                | Retrieval runs first; if nothing relevant is approved, the insufficient-knowledge answer is returned without a reservation. Charging a customer to be told their Brand Brain is empty would be indefensible.                                                                                                                                                              |
| **D-83** | **Prompt-injection containment applies to KNOWLEDGE, not only to document chunks.**                                                                                        | A candidate accepted from a poisoned document becomes a knowledge item, which retrieval trusts MORE than a raw chunk. Containment that stopped at the chunk boundary would be strongest exactly where the risk is lowest.                                                                                                                                                 |
| **D-84** | **`brand_brain.review` is a separate permission from `brand_brain.edit`, and `approver` now holds it.** No new role was introduced.                                        | Approving a candidate is what turns machine output into brand truth, and D-65 makes that a human decision with a named owner. Collapsing it into `edit` would mean anyone who can fix a typo can also approve everything a document proposed. `analyst` and `client_viewer` stay strictly read-only, including no `brand_brain.chat`, because a chat turn spends credits. |

### Phase 5A corrective pass (D-85 … D-97)

| ID       | Decision                                                                                                                                                                                                                                                                                                                                    | Reasoning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-85** | **The approved demo is a binding UI SPECIFICATION for every customer route, recorded in `docs/UI-FIDELITY-CONTRACT.md`.** The Brand Brain orb is re-ported mechanically from `demo/brand-brain-native.css` and `demo/brand-brain-native.js` at Landing commit `b01d947`.                                                                    | The first Phase 5A delivery re-interpreted the demo — a lavender container it does not have, a black rounded square where it has a transparent circle, white cards where it has 12px dots — and every automated check passed anyway. Accessible, correct, well-tested and wrong. Treating the demo as a specification removes the option.                                                                                                                                                                                                                                                                                 |
| **D-86** | **`prefers-reduced-motion` draws ONE STATIC FRAME of the orb.** An authorised deviation: the demo implements no reduced-motion path at all.                                                                                                                                                                                                 | `CLAUDE.md` §4 sets WCAG 2.2 AA as a product requirement and a prototype's omission does not outrank it. The accommodation shows the same orb and only stops animating, so a reader who asked for less motion still gets the knowledge map rather than a blank box.                                                                                                                                                                                                                                                                                                                                                       |
| **D-87** | **Brand Brain keeps SIX orbit nodes and TEN area cards below the hero.**                                                                                                                                                                                                                                                                    | The demo's orb carries exactly six nodes. The product has ten knowledge areas, all of which appear in the grid. Moving all ten into the orb because ten exist in the database would be a redesign, not a data binding.                                                                                                                                                                                                                                                                                                                                                                                                    |
| **D-88** | **Where a lint rule forbids a literal, a Brand Brain token carrying the demo's EXACT value is added** rather than substituting the nearest existing design-system token.                                                                                                                                                                    | Substituting an approximate token to satisfy a linter changes the approved result. It is a redesign wearing a compliance costume, and it is how the first delivery drifted.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **D-89** | **Five of the demo's small-text colours are darkened to the nearest step of the SAME hue that clears WCAG 2.2 AA**, listed with measured before/after ratios in `packages/ui/src/brand-brain.css` §2. Nothing else about them changes.                                                                                                      | `.bb-count` reads 2.85:1 and three greens read between 3.72:1 and 4.09:1 — a serious AA failure for anyone with low vision, at 8–9px. Darkening the same hue is the smallest change that fixes it; `.bb-count` takes the demo's own `--bb-muted`, so even the replacement is a value the demo chose.                                                                                                                                                                                                                                                                                                                      |
| **D-90** | **The chat lives INSIDE the hero's right-hand column and swaps with the stats view**, as the demo does, rather than floating over the page.                                                                                                                                                                                                 | The demo's `.bb-hero-stats.chat-open` hides the stats and shows the chat in the same fixed-height box. The floating panel in the first delivery changed the page's composition whenever chat opened, which is a different design and not a data binding.                                                                                                                                                                                                                                                                                                                                                                  |
| **D-91** | **The orbit nodes' position and scale are quantised to whole pixels and two decimal places.** A sub-pixel departure from the demo, which writes fractional values.                                                                                                                                                                          | A 12px target whose box changes on every frame cannot be clicked reliably by assistive tooling or by a browser's own click stabilisation, and Playwright refuses it outright — which is how the orb's own nodes went untested through Phase 5A. The rendered result is identical at any zoom a person uses.                                                                                                                                                                                                                                                                                                               |
| **D-92** | **A MOCK AI routing configuration is activated for development and end-to-end runs**, through the real Configuration Service with no validation relaxed, no real provider chosen and no credential resolved. The mock answers from retrieved knowledge outside production.                                                                  | Brand Brain chat had never been observed to produce an answer outside a unit test: with no active routing rule the gateway raises `RoutingError` and the panel shows the generic failure, correctly. A customer-facing capability that exists only in unit tests is not delivered.                                                                                                                                                                                                                                                                                                                                        |
| **D-93** | **Images are not a supported source format.** PNG and JPEG are removed from the `brand-brain` upload allow-list and no extractor claims them.                                                                                                                                                                                               | The only way to get text out of an image is OCR, and no option clears this feature's bar: the maintained JavaScript engine fetches its language model over the network at run time, its output is not reproducible across versions — which D-65 requires — and its Arabic accuracy is far below its Latin accuracy, so a bilingual product would be writing mistranscribed Arabic into approved knowledge. Refusing the upload is the honest answer until an option exists without those properties.                                                                                                                      |
| **D-94** | **Word and PowerPoint are read with a bounded ZIP reader and an element scan, not a document-conversion library; PDF is read with Mozilla's pdf.js (Apache-2.0), configured so it cannot compile, cannot fetch and cannot exceed its budget.**                                                                                              | The parser IS the attack surface for a file a stranger uploaded. A conversion library brings styles, numbering, image handling and HTML generation — all running on hostile bytes — to produce prose whose formatting we then discard. The smallest thing that does the job is a size- and ratio-checked inflate plus a scan for one element, and it makes XXE structurally impossible because no XML parser exists to attack.                                                                                                                                                                                            |
| **D-95** | **Queue definitions move to `packages/jobs`, and Brand Brain ingestion runs on the `media-processing` queue.** The dashboard writes the job row and dispatches; `apps/worker` consumes and parses.                                                                                                                                          | The definitions lived inside `apps/worker`, where an app may not import them — so a producer had nowhere to dispatch from, and Phase 5A parsed documents inside a server action by default rather than by choice. Inline parsing holds a request connection for the length of a parse, lets one slow document delay every other request on the instance, and loses the work on a deploy with nothing recording that it was lost.                                                                                                                                                                                          |
| **D-96** | **Background maintenance is scheduled in `apps/api`, not in the worker.** The reconciliation sweep and the retention purge enumerate across tenants; the per-tenant work is dispatched to the worker or performed inside that tenant's own RLS context.                                                                                     | Both sweeps begin with a cross-tenant question, and F-07 and the architecture keep the platform identity out of "ordinary workers". Putting the enumeration on the designated platform surface keeps the boundary intact instead of widening it for a maintenance job's convenience.                                                                                                                                                                                                                                                                                                                                      |
| **D-97** | **The development and test object store is backed by the local filesystem, not by process memory.** `createObjectStore` returns a filesystem store outside production, rooted at `BRANDSPACE_OBJECT_STORE_DIR` (default: a directory under the system temporary path). Production still refuses until a real storage adapter is configured. | D-95 split the producer from the consumer: the dashboard accepts the upload and `apps/worker` reads the bytes back in a DIFFERENT process. A `Map` in the dashboard heap is invisible to the worker, so every dispatched ingestion failed on a file that had just been written — the defect the end-to-end run caught only once the queue was actually taken. Keys are split into path segments and each is refused unless it matches a narrow alphabet, so a key can never address a directory outside the root; writes stage and rename, so a worker woken the instant the row commits cannot read a half-written file. |

### Phase 5B-1 — Asset Library (D-98 … D-109)

| ID        | Decision                                                                                                                                                                                                                                                                                                                        | Reasoning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-98**  | **A required screen with no approved reference is BUILT from the design system and recorded as an approved DESIGN-SYSTEM EXTENSION**, not stopped on and not invented freely. `CLAUDE.md` §4.2 and `docs/UI-FIDELITY-CONTRACT.md` §6 state the rule; `/[locale]/assets` is the first entry in its manifest.                     | The demo route for Media library is `simpleFeaturePage('media')` — a "Future product preview" kicker, a heading, one sentence and three identical placeholder cards. There is nothing to port, and §1 rule 2 as written invited two wrong readings: invent a screen, which rule 3 forbids, or refuse to build a required module until the owner designs it, which makes missing artwork an indefinite blocker on functional delivery. Composing from tokens, typography, spacing, surfaces, controls, drawers and states that already ship keeps the screen unmistakably part of this product; the cost is that it looks slightly generic, which a parity pass can correct and a screen with its own colour family cannot. |
| **D-99**  | **Every intra-Asset-Library foreign key is COMPOSITE with `workspaceId`** — the folder tree, the folder an asset sits in, and the asset a version, derivative, session or job belongs to.                                                                                                                                       | PostgreSQL performs referential integrity checks WITH RLS BYPASSED, so a plain key lets a tenant reference a row it cannot read. The isolation suite asserted that a tenant could not nest a folder under another tenant folder and GOT A CREATED ROW. Nothing is disclosed directly, but the insert succeeds only when the id names a real row, which turns any reference column into an existence oracle over the whole platform — the "infer" leak CLAUDE.md §2.1 names. Pairing each reference with the workspace collapses it to "is this a row in my own workspace".                                                                                                                                                 |
| **D-100** | **Duplicate protection is a PARTIAL unique index over the live rows** — `(workspaceId, checksumSha256) WHERE "deletedAt" IS NULL` — beside the plain index the model declares.                                                                                                                                                  | A plain index detects nothing on its own, and a full unique constraint would mean a customer who deleted a file could never upload it again, because the soft-deleted row keeps the checksum forever. The partial index refuses two live copies even when two requests race past the service check, and stops reserving the checksum the moment the row is deleted. Prisma cannot express a partial index, so it is raw SQL; `prisma migrate diff` does not model one, so there is no drift — verified against a fresh migrations-only database rather than assumed.                                                                                                                                                       |
| **D-101** | **A workspace-level asset (NULL `brandId`) is a first-class shape, not a degenerate one.**                                                                                                                                                                                                                                      | docs/DATABASE.md §4.6 says "null = workspace-level", and a multi-brand company's logo pack, font files and legal boilerplate genuinely belong to the workspace rather than to one of its brands. MATCH SIMPLE exempts the null case from the composite key by construction, so no special case was written — and the isolation suite asserts BOTH halves, because an exemption nobody tested is an exemption nobody can rely on.                                                                                                                                                                                                                                                                                           |
| **D-102** | **`asset_version` is append-only in three separated layers** — revoked privilege, FORCE RLS with no owner policy, and a trigger.                                                                                                                                                                                                | The same construction as `brand_knowledge_version`, for a sharper reason: restoring a version promises the customer "this is exactly the file you uploaded", and "exactly" is only true if nothing could have edited the row in between. The suite reaches the trigger by granting the privilege back exactly as a careless migration would, because the first two layers otherwise hide whether the third still works.                                                                                                                                                                                                                                                                                                    |
| **D-103** | **The download path is an opaque, HMAC-signed, time-limited GRANT** — never a filesystem path and never a provider object key. Forged, expired, malformed and wrong-workspace all refuse identically as `NOT_FOUND`.                                                                                                            | docs/SECURITY.md §11.6-11.7 requires a signed short-TTL URL and forbids serving from the application origin, and both are properties of the CONTRACT rather than of a vendor. Distinguishing "expired" from "forged" confirms the token was once real, which confirms the object exists. A real adapter redeems the same grant by issuing its own pre-signed URL; the shape does not change, which is the point of putting the boundary in `packages/storage`.                                                                                                                                                                                                                                                             |
| **D-104** | **The object store moved to `@brandspace/storage`, and Brand Brain re-exports it.**                                                                                                                                                                                                                                             | It lived in `packages/brand-brain` while Brand Brain was its only consumer. Two copies of a storage driver is how one product ends up with two key layouts, two safety checks and two answers to "may production run without a real adapter". The file was moved, not rewritten, so no caller changed.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **D-105** | **Production FAILS CLOSED until a real storage adapter is configured.** `createObjectStore()` returns the filesystem-backed store only outside production; with `NODE_ENV=production` and no `integrations.storage` adapter it throws rather than writing customer files to a container disk that the next deployment discards. | No storage vendor is approved (F-77), and the alternative to failing closed is pretending an upload is durable when it is not. A customer who uploads a logo and finds it gone after a deploy has lost data the product promised to keep. Refusing the upload is recoverable; accepting it and losing it is not.                                                                                                                                                                                                                                                                                                                                                                                                           |
| **D-106** | **Virus scanning ships with a DETERMINISTIC MOCK and nothing else, and production fails closed the same way.** `MockVirusScanner` recognises the EICAR test string and a small set of planted markers; it makes no claim about real malware. No scanner runs in production until one is selected and reviewed (F-78).           | A scanner is a network service or a native binary with its own update channel, and choosing one is a security, licensing and operations decision the owner has not made. A mock that returns CLEAN for everything in production would be worse than no scanner at all, because the quarantine gate would report a file as scanned and safe.                                                                                                                                                                                                                                                                                                                                                                                |
| **D-107** | **Derivative RECORDS exist; derivative BYTES do not.** `ENCODABLE_KINDS` is empty, so `planDerivatives()` plans nothing and the pipeline writes no thumbnails. The `asset_derivative` model, its RLS, its bounds and its lifecycle are complete and tested.                                                                     | Generating a thumbnail means decoding untrusted image bytes, which is the single most exploited surface in a media pipeline. Every candidate is a native dependency needing a maintenance, licence, security and offline-installation review that this task did not carry out (F-79). The schema is built so adding an encoder later is a configuration and worker change, not a migration.                                                                                                                                                                                                                                                                                                                                |
| **D-108** | **SVG is NOT in the allowed-type list.** It is absent from `AssetKind` detection and from the signature table, so an upload named `.svg` is refused with the same mismatch error any other undetectable type gets.                                                                                                              | SVG is an executable document: it carries script, external references and entity expansion. Accepting it means either sanitising it — another unreviewed dependency — or serving active content from the product's own origin. Refusing it is the honest position until the owner decides sanitisation is worth the surface.                                                                                                                                                                                                                                                                                                                                                                                               |
| **D-109** | **The tenant entitlement-catalogue projection is PARSED before it reaches the precedence engine.** `TenantCatalogueSource.load()` now runs `parseConfigPayload(domain, row?.payload ?? {})` rather than handing the raw JSON column through.                                                                                    | The projection is a JSON column, so a document written before a schema field existed — or by a fixture that omitted an optional array — reached `precedence.ts` with `undefined` where an array was expected and crashed on `.includes`. The platform-side reader already parsed; the tenant-side reader did not, which meant the two identities disagreed about the shape of the same document. Parsing on both sides makes schema defaults the single answer.                                                                                                                                                                                                                                                            |

### F-80 / F-83 corrective pass — Brand Brain composite foreign keys (D-112 … D-114)

| ID        | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Made by / source                                                        | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D-112** | **D-99 is not an Asset Library rule; it is the platform rule.** Every foreign key from a tenant-owned table to another tenant-owned table is composite with `workspaceId`, whichever module it lives in. All EIGHT Phase 5A keys — the three F-80 named and the five F-83 found — are now composite, and `brand_source_document`, `brand_brain_conversation` and `brand_knowledge_item` carry the `@@unique([workspaceId, id])` they reference. The rule is enforced by a test that asks the CATALOGUE whether any single-column foreign key to a tenant-owned parent remains, rather than by a list somebody maintains. | F-80 corrective pass                                                    | D-99 was written while designing the Asset Library and reads as a property of that module. It is not: the mechanism it defends against — referential integrity evaluated with RLS bypassed — is a property of PostgreSQL, so every module inherits it. Stating it once, generally, is what stops the next module rediscovering it the way Phase 5B-1 rediscovered it in Phase 5A.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **D-113** | **A migration that VALIDATES tenant data lifts `FORCE ROW LEVEL SECURITY` for the duration of its own transaction, and asserts it back on before committing.** The F-80 migration does this for the five Brand Brain tables it touches.                                                                                                                                                                                                                                                                                                                                                                                  | F-80 corrective pass (measured against a real PostgreSQL, not reasoned) | These tables are FORCE RLS with policies for `brandspace_app` and `brandspace_platform` only, and migrations run as the OWNER, `brandspace_migrator`, which no policy names. The owner therefore sees ZERO ROWS — so a pre-flight anti-join returns 0 offending rows however many exist, and, far worse, `ALTER TABLE … ADD CONSTRAINT FOREIGN KEY` validates only the visible rows and still marks the constraint `convalidated = true`. The migration would have reported success over data that violates it. `ALTER TABLE` holds an ACCESS EXCLUSIVE lock to commit, so no other session can observe the lifted state, and a `DO` block refuses to commit unless all five tables are ENABLED and FORCED again. This is PostgreSQL's own prescribed remedy — `SET LOCAL row_security = off` fails here with a HINT naming it.                                                                                                                                                                                                                                                                                                                                                                                                 |
| **D-114** | **A composite `ON DELETE SET NULL` NAMES THE COLUMN IT NULLS.** Every workspace-scoped key whose action is `SET NULL` is written `ON DELETE SET NULL ("<reference column>")`, never bare, so a referential action can never write the tenant key. Three keys use it: `brand_knowledge_candidate.targetItemId`, `brand_knowledge_item.sourceDocumentId` and `brand_knowledge_item.conflictsWithItemId`.                                                                                                                                                                                                                   | F-83 (measured against a real PostgreSQL)                               | Making a key composite changes what `SET NULL` means. PostgreSQL nulls EVERY referencing column of the key, so `(workspaceId, targetItemId)` would null `workspaceId` too — and because `workspaceId` is `NOT NULL` the parent delete does not corrupt the tenant key, it FAILS: deleting a knowledge item that any candidate targets, or a source document any item cites, would have started erroring on a path that worked the day before. A leak fix that introduces a data-loss-shaped defect is not a fix. The column list (PostgreSQL 15+; `docs/ARCHITECTURE.md` targets 16+ and CI runs 16) restricts the nulling to the one nullable reference. Prisma has no syntax for it, so `prisma validate` warns "`onDelete: SetNull` … when a referenced field is required" on these three relations — that warning is Prisma describing the SQL IT would emit, which is precisely why the migration is hand-written. `prisma migrate diff` does not model `confdelsetcols`, so there is no drift (same precedent as D-100's partial index), verified against a fresh migrations-only database rather than assumed. The migration itself refuses to commit if any such key lacks a single-column list or names `workspaceId`. |

## 6. Risk Register

| Risk                                              | Likelihood | Impact       | Mitigation                                                                                                                                                          |
| ------------------------------------------------- | ---------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Social platform app review delays launch          | **High**   | High         | Start reviews in Phase 1–2 (D-19); design so launch can proceed with a subset of platforms                                                                          |
| Arabic AI output quality below expectation        | Medium     | **High**     | Evaluate models on Arabic marketing copy before Phase 4 (D-17); Brand Brain grounding; multi-provider routing so a better model can be swapped in without a release |
| AI provider price increase erodes margin          | Medium     | High         | Credit abstraction insulates customers; margin monitoring and alerts; routing can move traffic to cheaper models instantly                                          |
| Tenant isolation defect                           | Low        | **Critical** | Two enforcement layers, mandatory isolation tests, CI coverage gate, penetration test before launch                                                                 |
| Credit accounting bug (over/under charging)       | Medium     | High         | Immutable ledger, reserve/settle, concurrency tests, nightly reconciliation with alerting                                                                           |
| Duplicate social posts damaging customer trust    | Medium     | High         | Idempotency keys, verify-don't-retry on uncertain outcomes, per-target job identity                                                                                 |
| Scope creep delaying the MVP                      | **High**   | Medium       | Phase gates with explicit exit criteria; the vertical slice is deliberately narrow                                                                                  |
| Secret leakage                                    | Low        | **Critical** | Vault abstraction, no-reveal UI, redaction layer, CI secret scanning, bundle scanning                                                                               |
| Social platform API breaking changes              | High       | Medium       | Independent connectors, contract tests with recorded fixtures, per-platform disable switch                                                                          |
| Small team, broad scope                           | **High**   | High         | Modular monolith, shared language, heavy reuse of managed services, strict phase discipline                                                                         |
| Payment provider unavailable in the target market | Medium     | High         | Provider abstraction and the option of a second regional provider (D-21)                                                                                            |
| Prompt injection via uploaded brand documents     | Medium     | High         | Untrusted-data delimiting, server-side per-call authorization, allowlisted tools, output validation                                                                 |

---

## 7. Follow-Up Items Raised During Implementation

Recorded here so they are decided deliberately rather than forgotten.

### Phase 5A additions (Brand Brain)

| ID                    | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Raised in                          | Recommendation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | When                                                 |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| **F-68**              | **The AI Gateway needs the PLATFORM database identity, so a customer-initiated AI action cannot execute in the dashboard.** Phase 5 is the first time a customer action triggers an AI request. The gateway reads platform-owned `ai.*` configuration and settles credits in its own transactions outside any tenant context; F-07 forbids the customer dashboard from holding that identity. Three independent blockers point the same way: the configuration domains are unreadable to `brandspace_app`, `asPlatform()` requires a platform actor with verified MFA and is not a path a customer action can take, and the gateway's own transactions would run with `app.current_workspace_id()` NULL. | Phase 5A (W5)                      | **Resolved:** customer-initiated AI executes in `apps/api`, the designated platform surface. The route uses the TENANT identity under `withWorkspace` for every Brand Brain table and the PLATFORM identity for the gateway alone. Projecting `ai.*` into the tenant-readable snapshot (the D-44 precedent) was considered and REJECTED: `ai.providers` carries provider names and credential references, and CLAUDE.md §10 says customers are never shown provider names.                                                                            | **Done** (Phase 5A)                                  |
| ~~F-69~~ **RESOLVED** | **Ingestion processing runs inline in the request.** There is no background job runner wired to the customer application, so a queued job would sit untouched and the customer would watch a spinner that never resolves.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Phase 5A (W3)                      | **Done** (Phase 5A corrective pass). Queue definitions move to `packages/jobs` so a producer can reach them (D-95); the dashboard writes the durable row and dispatches to `media-processing`, and `apps/worker` consumes it. Production cannot process inline at all; outside production a missing `REDIS_URL` still falls back, so a developer needs no Redis container. A reconciliation sweep in `apps/api` re-dispatches unclaimed work, so a lost message costs punctuality rather than correctness.                                            | **Done**                                             |
| ~~F-70~~ **RESOLVED** | **No document extractor for PDF, DOCX or PPTX, and no OCR.** Each is a third-party dependency nobody has reviewed, and the ones that matter are exactly the formats customers will upload most.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Phase 5A (W3)                      | **Done** (Phase 5A corrective pass). PDF through pdf.js and Word/PowerPoint through a bounded ZIP read and element scan (D-94), each with configured page, character, archive and wall-clock ceilings, signature validation that refuses a renamed file, and refusals for macros, embedded executables, path escapes and decompression bombs. OCR is declined with reasons (D-93), so images stay unsupported and are refused at upload. 29 unit tests and 6 isolation tests.                                                                         | **Done**                                             |
| ~~F-71~~ **RESOLVED** | **Brand Brain policy is read from schema defaults, not from the activated configuration.** The `brand-brain` configuration domain exists and validates, but the tenant role cannot read `configuration_version`, so the dashboard uses the schema's own defaults.                                                                                                                                                                                                                                                                                                                                                                                                                                        | Phase 5A (W5)                      | **Done** (Phase 5A corrective pass). `brand-brain` is projected into `entitlement_catalogue_snapshot`; the CHECK constraint admits it (and `credits`, which had been listed in code since Phase 3 without the constraint allowing it). No application source carries a policy value any more, and a unit test refuses the reintroduction of one.                                                                                                                                                                                                      | **Done**                                             |
| ~~F-72~~ **RESOLVED** | **Retention purge has no scheduler.** `purgeExpiredChatContent()` exists and is tested, but nothing calls it on a timer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Phase 5A (W7)                      | **Done** (Phase 5A corrective pass). `MaintenanceScheduler` in `apps/api` runs it on a configured cadence, together with the gateway own output purge (D-96 explains why not the worker). Both clear content and keep the row, so the accounting survives; both are bounded, idempotent and driven by an injected clock, which is what let the window be tested in both directions rather than only the positive one.                                                                                                                                 | **Done**                                             |
| ~~F-74~~ **RESOLVED** | **`Membership.brandScope` is ignored by every Phase 5A code path.** `docs/SECURITY.md` §4.2 defines brand-scope resolution as "membership role AND brand in `brandScope`". Phase 5A introduces the first brand-scoped resources, and nothing consults `brandScope`: a member may name any `brandId` in their own workspace.                                                                                                                                                                                                                                                                                                                                                                              | Phase 5A security review           | **Done** (Phase 5A corrective pass). `assertBrandInScope` / `brandInScope` / `brandScopeFilter` in `packages/shared`, enforced inside the SERVICES rather than at each call site: `KnowledgeActor.brandScope`, `UploadInput.actorBrandScope` and the chat send are all REQUIRED fields, so a new call site that forgets the scope is a type error rather than a silent grant. An out-of-scope brand answers `NOT_FOUND`, identical to a miss. 12 isolation tests against a second brand in the SAME workspace, which is the grain RLS does not cover. | **Done**                                             |
| **F-75**              | **`apps/api` now serves a customer-authenticated route while holding the platform credential.** It was already a platform surface, so no boundary moved, but the blast radius of any future flaw there is now every tenant rather than one.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Phase 5A security review           | The route keeps the identities strictly apart today. If the customer-facing AI surface grows beyond one endpoint, split AI execution into its own process so the platform credential is not co-resident with customer session handling. `.env.example` has been corrected: it previously read as though the API served no tenant-facing traffic.                                                                                                                                                                                                      | Revisit when the second customer AI endpoint lands   |
| **F-76**              | **Shared customer shell and remaining page-level visual parity are deferred.** The Brand Brain content surface and orb match the pinned native implementation, but the current shared sidebar and top bar still differ from the pinned `demo/index.html` / `brand-brain-nav.js`. The final capture also does not prove the full page below the fold, and the chat baseline shows minor quick-prompt clipping.                                                                                                                                                                                                                                                                                            | Phase 5A independent visual review | Keep the functional Phase 5A delivery. Before Phase 5 is declared visually complete, mechanically port the shared customer shell once (never as a route-specific duplicate), correct the remaining overflow, and extend measured parity coverage to the shell, top actions and the complete page in desktop/mobile and RTL/LTR.                                                                                                                                                                                                                       | Final Phase 5 UI parity pass, before Phase 5 closure |
| **F-73**              | **`persistOutput` still has no customer-facing control.** F-63 is closed for Brand Brain chat — the notice is on the panel before the customer types — but the notice is per-feature, not a setting the customer can change, and §11 describes opt-in retention.                                                                                                                                                                                                                                                                                                                                                                                                                                         | Phase 5A (W7)                      | A workspace-level retention control belongs with the settings screen, once more than one feature persists output.                                                                                                                                                                                                                                                                                                                                                                                                                                     | Phase 5B                                             |

| ID                    | Item                                                                                                                                                                                                                                                                                   | Raised in                                     | Recommendation                                                                                                                                                                                                                 | When                                                                                                                                                                                                                                     |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~F-01~~ **RESOLVED** | **`asPlatform()` hardening.** The platform-mode flag is a transaction-local PostgreSQL setting. It is a strong control against developer error and it produces the audit trail, but an attacker who already has arbitrary SQL execution as the application role could set it directly. | Phase 1 (`packages/database/src/platform.ts`) | Give platform operations their own database role and connection pool, so cross-tenant visibility is a property of the connection rather than a session variable. Stated honestly in the code comment rather than assumed away. | **Done** (Phase 1 hardening). Implemented as a separate `brandspace_platform` role and pool with role-targeted RLS policies; the GUC function is dropped. 24 dedicated tests in `tests/isolation/platform-role.test.ts`.                 |
| F-02                  | **`User` reads with no tenant context.** Authentication must resolve a user by email before any workspace is known, so the `user` RLS policy permits reads when no workspace context is set. Inside a workspace context, only that workspace's members are visible.                    | Phase 1 (RLS migration §4.6)                  | Keep the exception and keep it narrow — authentication is the only unscoped code path. Revisit if another caller ever needs it.                                                                                                | Reviewed in Phase 2                                                                                                                                                                                                                      |
| F-03                  | **Prisma shadow database requires `CREATEDB`** on the migrator role for `prisma migrate dev`.                                                                                                                                                                                          | Phase 1                                       | Use a dedicated shadow-database URL in staging and production instead of granting `CREATEDB`. The runtime application role is unaffected: it remains `NOBYPASSRLS` and owns nothing.                                           | Phase 2 deployment work                                                                                                                                                                                                                  |
| F-06                  | **Verification must run in a clean shell.** The PR #2 defect passed local checks only because `.env` was exported into the developer shell, masking a hard dependency. Three CI jobs failed on a clean checkout.                                                                       | Phase 1 hardening                             | `tests/unit/prisma-config.test.ts` now runs the Prisma CLI with a scrubbed environment, so this class of bug fails a test rather than CI. Before reporting a chain green, run it once with nothing exported.                   | Done                                                                                                                                                                                                                                     |
| ~~F-04~~ **RESOLVED** | **Playwright E2E not yet wired.** Approved in D-01, but there are no user-facing flows to exercise in Phase 1.                                                                                                                                                                         | Phase 1                                       | Add with the first real screens, covering RTL and LTR plus an accessibility smoke test, as the Phase 1 exit criteria in `docs/ROADMAP.md` require.                                                                             | **Done** (Phase 1 hardening). 114 Playwright tests across all three interfaces: RTL/LTR, navigation, keyboard reachability, visible focus, responsive overflow, and axe accessibility failing CI on serious/critical.                    |
| ~~F-05~~ **RESOLVED** | **OpenTelemetry spans not exporting.** Structured logging with redaction and correlation fields was in place; span export was not.                                                                                                                                                     | Phase 1                                       | Wire the OTLP exporter once there are real routes and a collector endpoint exists.                                                                                                                                             | **Done** (Phase 2A). `packages/observability` installs a `NodeTracerProvider` with a batched OTLP exporter, disabled safely when unconfigured. Every attribute passes a redaction layer. 45 tests in `tests/unit/observability.test.ts`. |

| F-07 **MITIGATED, still open** | **The platform credential is the new boundary.** Anyone holding `DATABASE_PLATFORM_URL` has cross-tenant access. | Phase 1 hardening (F-01) | Phase 2A narrowed _who can reach it in this codebase_ with four independent controls: `import 'server-only'` on the admin server context (a client-component import is a build error), an ESLint rule limiting `@brandspace/database/platform` to apps/admin and apps/api, the pool's own browser guard, and unit tests that assert all of the above in both directions. What is NOT yet done, and is what would actually close this: short-lived credentials from a secret manager, and splitting read-only from read-write platform access. | Short-lived credentials: Phase 6 (deployment). Read/write split: revisit when a platform read-only role has a caller. |
| ~~F-08~~ **PARTLY RESOLVED** | **Playwright covered scaffold pages only.** The suite proved infrastructure (direction, landmarks, keyboard, contrast, overflow) rather than user journeys. | Phase 1 hardening (F-04) | Grow the suite with each real screen. | **Phase 2A**: 47 admin journeys — sign-in, MFA enforcement, the configuration lifecycle, optimistic concurrency, secret storage and rotation, RTL/LTR, keyboard operation, axe, overflow. The MVP vertical slice in `docs/MVP-ACCEPTANCE-CRITERIA.md` §19 remains uncovered because the customer flows it describes do not exist yet. |
| F-09 | **The KMS key provider is an interface, not an implementation.** `KmsKeyProvider` throws rather than pretending to work, and `createKeyProvider()` refuses the local development provider when `NODE_ENV=production`. | Phase 2A (`packages/secrets/src/key-provider.ts`) | Choose a KMS at deployment time (AWS KMS, GCP KMS, or the hosting provider's equivalent) and implement `wrapDataKey` / `unwrapDataKey` against it. The envelope-encryption shape does not change: only the key-encryption key moves. **Production cannot start today without this choice**, which is deliberate — failing to start beats encrypting with a key derived from an environment variable. | **Owner decision needed before production.** Phase 6. |
| F-10 **FIXED IN THIS PHASE** | **`platform_user` was readable by the tenant application role.** The identity migration created the table and the RLS migration then granted `brandspace_app` every privilege on ALL TABLES IN SCHEMA public. No policy was ever added, so `SELECT "passwordHash", "mfaSecretRef" FROM platform_user` succeeded as the tenant role — any SQL-injection defect in a customer request path would have exposed the Platform Owner's Argon2id hash. | Phase 2A (found while writing `tests/isolation/platform-auth.test.ts`) | Fixed by migration `20260901210500_platform_user_isolation`: RLS enabled and forced, a policy naming `brandspace_platform` only, and every privilege revoked from `brandspace_app`. The class of bug is closed by the isolation gate now requiring EVERY model to be classified — a model nobody classified fails the build instead of silently defaulting to "not tenant data". | **Done** |
| F-11 | **Several drafts can exist per configuration domain at once**, and the console edits the newest. | Phase 2A (`packages/config/src/service.ts`) | Not a defect — two administrators drafting in parallel is legitimate — but the console shows only one of them, which is confusing. Either surface all open drafts, or require one draft per domain and say so. | Phase 2B, with the per-domain form editors (D-30). |
| F-12 | **Configuration cache invalidation is per-process.** `InMemoryConfigCache` has a 30-second TTL; activating on one instance leaves other instances serving the previous payload until their entries expire. | Phase 2A (`packages/config/src/service.ts`) | Acceptable at one instance and bounded at 30 seconds beyond that. Before running multiple instances, publish activation on Redis pub/sub and invalidate on receipt. | Phase 6 (deployment), or sooner if a second instance is added. |
| **R-01** ~~**RESOLVED**~~ | **CRITICAL — MFA lockout could be bypassed.** `verifyMfa()` never read `lockedUntil`. Failed MFA attempts set the lock, but nothing consulted it, so a session created before the lock could keep submitting codes — and a CORRECT code still signed the attacker in. The counter was also read-modify-write, so parallel attempts overwrote each other and the threshold was never reached. | Independent security review of PR #4 | Enforce the lock inside `verifyMfa()`, revoke pre-MFA sessions when the lock fires, and make the counter a single atomic UPDATE. Never swallow a failure to persist it. | **Done.** 15 regression tests in `tests/isolation/platform-lockout.test.ts`; 9 of them failed against the vulnerable code. |
| **R-02** ~~**RESOLVED**~~ | **CRITICAL — read-only roles could modify secrets and configuration.** Every secret and configuration action was gated on `platform.workspace.read` ("View any workspace"), held by `support_agent`, `billing_manager` and `operations_viewer`. The services checked only that an actor existed and had MFA, so calling them directly bypassed RBAC entirely. | Independent security review of PR #4 | Five new least-privilege permissions (D-35), enforced at page, action AND service level (D-36). | **Done.** 42 table-driven tests in `tests/isolation/platform-rbac.test.ts` plus 14 in `tests/unit/rbac-matrix.test.ts`; 29 failed against the vulnerable code. |
| **R-03** ~~**RESOLVED**~~ | **HIGH — one recovery code could succeed twice concurrently.** `findMany` then `update` by id let two requests read the same unused code and both win. | Independent security review of PR #4 | Consume with `UPDATE ... WHERE usedAt IS NULL` and require exactly one affected row (D-39). | **Done.** Concurrency tests in `tests/isolation/platform-lockout.test.ts`; the two-request test returned `['ok','ok']` before the fix. |
| **R-04** ~~**RESOLVED**~~ | **HIGH — a hard-coded development owner password.** `seed.ts` fell back to a literal committed value. | Independent security review of PR #4 | Remove the fallback; require an explicit, strength-checked variable; fail closed (D-38). | **Done.** 17 tests in `tests/unit/seed-password.test.ts`. |
| **R-05** ~~**RESOLVED**~~ | **MEDIUM — raw internal errors were placed in redirect URLs.** `safeMessage()` returned `error.message`, which then travelled through the address bar, browser history, access logs and `Referer` headers. | Independent security review of PR #4 | Emit only fixed codes plus an opaque correlation id (D-37). | **Done.** 21 tests in `tests/unit/public-error.test.ts` and 22 in `tests/unit/error-boundary.test.ts`. |
| **R-05a** ~~**RESOLVED**~~ | Two adjacent gaps the R-05 boundary tests exposed: `redact()` did not catch a connection string with embedded credentials inside an error MESSAGE (only under a sensitive key), and `sanitizeAttributes` did not drop `error.detail`-style span attributes. | Found while writing the R-05 regression tests | Add a URL-with-credentials value pattern to the shared redaction layer, and treat error detail/stack as forbidden span attributes. | **Done.** |
| F-13 | **Recovery codes are shown once, at enrolment, and cannot be regenerated from the Control Center.** Lockout itself is implemented (10 failed attempts across either factor locks the account for 15 minutes; the refusal is indistinguishable from a wrong password), but an operator who exhausts their codes has no self-service route back. | Phase 2A | Add a recovery-code regeneration screen behind a step-up check, and an operator-visible list of locked accounts. | Phase 2B. |
| F-14 | **Conditional cells in the role matrices are ungranted, not implemented.** docs/SECURITY.md §4.3–§4.4 mark several authorities as 🟡 (Marketing Manager invites "non-admin roles"; Support Agent resends invitations and grants goodwill credits "within cap"; Billing Manager suspends "for non-payment"). The condition — the cap, the role filter — does not exist yet, so the plain permission is NOT granted. | Phase 2B implementation | An ungranted capability is recoverable; an ungated one is not. Implement the conditions, then grant. Asserted as ungranted in tests/unit/phase2b-boundaries.test.ts. |
| F-15 | **`workspace_admin` was defined as "every workspace permission except two", so it silently inherited `billing.manage` the moment that permission was added** — an authority docs/SECURITY.md §4.3 reserves for the Owner. Found by the new RBAC matrix test before it shipped; the grant is now an explicit deny list. Any future blanket grant has the same defect. | Phase 2B implementation | Fixed. Recorded because the PATTERN is the risk, not the single instance. |
| F-16 | **Support Mode has no elevated write grant.** The Blueprint allows one (docs/SECURITY.md §8, "write actions require a separate elevated grant"); Phase 2B issues none, so `assertMayWrite()` always refuses and audits. | Phase 2B implementation | Deliberate: read-only is the safe default, and a write path with no reviewer is worse than none. Needs an owner decision on which writes support may perform. |
| F-17 | **Customer MFA is optional and not yet implemented.** docs/SECURITY.md §3 makes it optional for customers and mandatory for platform roles; only the platform half exists. | Phase 2B implementation | The customer identity model already carries `mfaEnabled`/`mfaSecretRef`, so this is additive. |
| F-18 | **Email verification is not enforced before a customer's first sign-in.** docs/SECURITY.md §3 requires it. Invitation acceptance proves control of the address, which covers the invited path; self-serve sign-up does not exist yet. | Phase 2B implementation | Close it with self-serve registration, which is not in Phase 2B scope. |
| F-19 | **Rate limiting is per-account, not per-IP.** Both the platform and customer realms lock an account after 10 failed attempts; neither throttles by source address, so an attacker can lock a known account (bounded denial of service) and can spread attempts across many accounts. | Phase 2B implementation | docs/SECURITY.md §10 specifies per-IP limits in Redis. Needs the rate-limit infrastructure, which no phase has built yet. |
| F-20 | **A session-token parameter was passed a user id, and the failure was SILENT.** The workspace picker called `listWorkspaces(customer.userId)` where a session token belongs. Both are `string`, so it typechecked; the query matched no session and returned `[]`, which the page rendered as "no workspace is available to your account". Every authenticated end-to-end test failed for a reason that read like missing data. | Phase 2B implementation (found by the E2E suite) | Fixed at the call site AND at the API: an unknown token now raises `UNAUTHENTICATED` rather than returning an empty list (D-45), and the dashboard redirects to sign-in. The residual risk is the PATTERN — two unrelated `string` parameters that are silently interchangeable. A branded `SessionToken` type across the auth package would close it structurally. | Branded token types: next auth change. |
| F-21 | **A whole feature was tested only through the WRONG database role.** Every invitation redemption test ran on the PLATFORM client, which is governed by `platform_access`. The customer application connects as the tenant role, where no policy permitted the read — so `peek()` and `accept()` returned nothing and every invitation link in the product was dead while the suite stayed green. | Phase 2B implementation (found by the E2E suite, not by the isolation suite) | Fixed by D-46, and the redemption path is now asserted on the tenant role. The residual risk is the PATTERN: a service tested on a more privileged client proves nothing about the role that will run it. Worth a sweep of every service the customer application calls, checking each has at least one test on `appRoleClient()`. | Sweep: before the next phase's PR. |
| F-22 | **`INSERT ... RETURNING` is subject to the SELECT policy, and reports the failure as a WRITE error.** A workspace-less outbox row is legitimately writable with no context and deliberately not readable, so Prisma's `create()` failed with "new row violates row-level security policy" for a write that policy actually allowed. | Phase 2B implementation | Fixed by generating the id and using `createMany` (no RETURNING), as the audit path already does. The residual risk is the PATTERN: any table whose `WITH CHECK` is wider than its `USING` will do this. Worth a check of every write-only widening before the next phase. | Sweep: with the F-21 sweep. |
| F-23 | **Isolation suites must BOOTSTRAP what they read, never assume a seed.** CI runs migrations only, so the Phase 2B suites — which read `workspace_admin`, `analyst` and the workspace permission catalogue — passed on a seeded developer machine and failed on a fresh database. The same class of bug as F-06, in the fixtures rather than the environment. | Phase 2B implementation (found by re-running the suite against a wiped schema) | Fixed by `ensureWorkspaceRbac()` in `tests/isolation/fixtures.ts`, called from `createIsolationFixtures`, mirroring the platform catalogue helper. Verify by dropping the schema and running the whole suite, not by trusting a local database that has been seeded at some point. | **Done.** |
| F-24 | **Two style systems had already drifted.** `console-ui.tsx` and `workspace-shell.tsx` implemented the same eight primitives twice, with different input widths, different table borders, one `Field` component, and one table keyboard-scrollable and the other not. The console's `h1` also still used the marketing identity blue on a screen the owner had approved as purple. | Phase 2C-A audit | Both files are now thin adapters over `@brandspace/ui`. The adapters are a MIGRATION SURFACE with a scheduled end, not an API: Phase 2C-B replaces the call sites and deletes them. | Phase 2C-B. |
| F-25 | **A pure function exported from a `'use client'` module cannot be called by a server component.** React treats every export of a client module as a client reference, so `menuItemStyle()` failed at runtime and every authenticated page returned a 500. A function prop (`renderLink`) crossing the same boundary failed for a related reason. | Phase 2C-A implementation (found by the E2E suite) | Fixed by moving pure helpers and data to neutral modules and by making the shell render `next/link` itself from serialisable items. The residual risk is the PATTERN: nothing yet fails the build when a helper is added to a `'use client'` module. A lint rule would close it. | **Done** (Phase 2C-A visual revision): `tests/unit/design-system.test.ts` reads the first line of every module in `packages/ui/src` and fails any `'use client'` module that exports anything but a PascalCase component, a `use…` hook or a type. A structural test rather than a lint rule: the property is about a module's first line and its export names together, which is awkward to express in ESLint and impossible to forget when it fails the suite. |
| F-26 | **`scrollWidth − clientWidth` is not a reliable overflow measurement.** It counts content parked at negative offsets — Next.js positions its route announcer at `left: -10px` — and reported a 10px overflow on pages that had none, while also masking which element was responsible. | Phase 2C-A implementation | **Done** (Phase 2C-A visual revision). The measurement moved to `tests/e2e/overflow.ts` and ALL FOUR suites now use it — design-system, customer-app, admin-console and responsive. Writing it down also surfaced the worse half of the defect: `scrollWidth` is **blind to the direction that matters in Arabic**, because content pushed past the LEFT edge produces a negative offset rather than a larger scroll width. The proxy could not see the regression it existed to catch. | **Done.** |
| F-27 | **Forty-three form controls were rendered INVISIBLE by the token change, and nothing failed.** Under the borderless model a control's fill, hover, disabled state and focus ring all come from the `.bs-control` class; `inputStyle()` supplies only geometry. Forty-three inputs and selects across the two consoles were styled without that class, so they became transparent rectangles on a white page — legible only once focused. Typecheck, lint and every existing test passed. | Phase 2C-A visual revision (found by auditing the controls, not by a test) | All forty-three now carry the class, and `tests/unit/design-system.test.ts` scans every `<input>`, `<select>` and `<textarea>` in both applications and in `packages/ui`, failing any that is styled with a control helper and missing the class. The residual risk is the PATTERN: a styling system where a missing class means "invisible" rather than "slightly off" needs the guard to stay. | **Done.** |
| F-28 | **A container `opacity` is a silent contrast failure.** `opacity: 0.85` on a feature card to make it read as "quieter" blended its state badge from `#175CD3` toward the page to an effective `#3a74da` on `#f1f9ff` — 4.2:1, an AA failure — with no colour changed anywhere in the source. The same device on the calendar's out-of-month cells would have done it again. | Phase 2C-A visual revision (found by axe on the showcase) | Both removed. Quieting is done with a softer surface and a muted-but-measured text colour. The residual risk is that `tests/unit/contrast.test.ts` measures TOKENS and cannot see a composite opacity, so only the axe run catches this class; it is documented in `docs/DESIGN-SYSTEM.md` §2 as a rule rather than left to memory. | Rule documented; axe covers it. |
| F-29 | **An ARIA role brings required structure with it, and a CSS grid does not supply it.** The calendar's `role="grid"` parented `columnheader` and `gridcell` elements directly, because CSS grid needs no row elements — axe reported `aria-required-children` and `aria-required-parent` across 36 nodes. Separately, a tab strip used as a filter left every `aria-controls` pointing at a panel that was never rendered. | Phase 2C-A visual revision (found by axe on the showcase) | Weeks are now wrapped in `role="row"` with `display: contents`, which adds the semantics without adding a box; the posts library renders a real `TabPanel` per status and genuinely filters the records. The residual risk is the PATTERN: a role added for semantics without its companions. | **Done.** |
| F-30 | **The same min-width bug returned as soon as the sidebar widened.** A grid item defaults to `min-width: auto`, so a card holding a table with a `min-inline-size` refuses to shrink and pushes the page sideways — 204px at 768px in the first draft, fixed in `Stack`; 244px again in this revision once the sidebar went from 16rem to 17rem. | Phase 2C-A visual revision (found by the design-system suite) | The floor now lives in `cardStyle()` (`min-inline-size: 0`) rather than in each caller, so a card cannot be laid out without it. | **Done.** |
| F-31 | **No application had a favicon, and the browser's automatic `/favicon.ico` probe was a 404.** Invisible under `chrome-headless-shell`, which does not make the request; the smoke suite's "no browser errors" assertion caught it the moment a full Chromium build was used. | Phase 2C-A visual revision | All three applications now ship `src/app/icon.svg`, so Next.js emits a `<link rel="icon">` and the probe never happens. Worth noting as a class: an assertion that passes only under one browser build is an assertion with a hole in it. | **Done.** |
| F-32 | **A text token measured against ONE ground is not measured.** `textMuted` is 4.61:1 on white — the only surface it had ever been asserted against — and 4.21:1 on `surfaceLavenderStrong`, where it shipped onto the composer's selected account chip. The revision made supporting surfaces the primary structural device, so body text now lands on eight grounds instead of one, and every existing pairing assertion covered only the first. | Phase 2C-A visual revision (found by axe on the showcase) | The chip uses `textSecondary`. `tests/unit/contrast.test.ts` now asserts the whole matrix — every text token against every supporting surface — and names the exactly two surfaces `textMuted` may not sit on, failing if a palette change moves one across that line. | **Done.** |
| F-33 | **A Playwright project documented as "not run by default" ran on every CI push.** The comment beside the `visual-review` project said `pnpm e2e:screenshots` invokes it; Playwright runs every project in the array unless `--project` is given, so it ran in `pnpm test:e2e` too — regenerating 55 screenshots on a CI runner that discards them when it is torn down. Not a correctness failure: the run on `a57e3de` passed all seven checks in about six minutes. But the comment asserted the opposite of what the code did, and a capture project that writes files into the repository has no business in a behavioural test run. | Phase 2C-A visual revision | The intent is now implemented rather than described: the project is registered only when `BRANDSPACE_VISUAL_REVIEW=1`, which `pnpm e2e:screenshots` sets. `pnpm test:e2e` runs 188 behavioural tests; producing the evidence is a separate, local command. Every job also gained a `timeout-minutes` ceiling — ordinary hygiene, so a future hang fails in bounded time with its report uploaded rather than inheriting the six-hour default. | **Done.** |
| F-34 | **GitHub's check-run and job endpoints reported a COMPLETED job as `in_progress` for over two hours.** Run 17 finished successfully at 02:51:31; the job endpoint was still returning `in_progress`, with frozen step timestamps, long afterwards. Acting on that reading, I diagnosed a hang that had not happened and wrote it up as a defect. | Phase 2C-A visual revision (my own misreading, recorded so the next person does not repeat it) | **The run-level `conclusion` is the authority, not the per-job `status`.** When judging whether CI is stuck, read `GET /actions/runs/{id}` and compare `run_started_at` with `updated_at`; a run whose `updated_at` has advanced past its start has progressed regardless of what the job endpoint claims. The changes F-33 describes stand on their own merits — they were not needed to fix a hang, because there was no hang. | **Recorded.** |
| F-35 | **A page-level `overflow-x: hidden` blinds the overflow suite.** Copied from the approved reference so the ambient orbs could not widen a phone, it made `inlineEndOverhang` return 0 for a deliberately planted 900px element — the meta-assertion that exists to prove the measurement can fail was the only thing that caught it. A page-level clip does not fix overflow, it hides it from the reader and from every automated check at the same time, which is the F-28 shape exactly. The clip belongs on the orbs' own fixed container, and the helper now refuses to treat `html` or `body` as a legitimate clipping ancestor. | Phase 2C-A demo alignment | Removed. `tests/e2e/overflow.ts` hardened, and it immediately reported a real 11px overhang in Arabic that the clip had been hiding (F-36). |
| F-36 | **A metric value set its card's width.** `MetricCard.value` is a formatted string and is not always a number — a plan key and an environment name render there too. At the reference's 32px numeric step the word `DEVELOPMENT` is 253px, which pushed the whole Control Center 11px off the inline-end edge in Arabic; wrapping it then broke it mid-word into `DEVELOPME / NT`, a card that no longer overflows but is still broken. | Phase 2C-A demo alignment | A value over nine characters takes the section step instead of the numeric one, and wraps rather than pushing. |
| F-37 | **A stored display preference can silently undo an explicit one.** The sidebar's collapse state is read from `localStorage` in a mount effect, which necessarily runs after first paint — so a click landing in that frame was overwritten and the sidebar sprang back open. Rare in a browser and rare in a test, which is what makes it the worst kind of bug to leave in a control. | Phase 2C-A demo alignment | An explicit choice now always wins over the stored one. Surfaced as a single non-reproducing suite failure and fixed rather than re-run. |
| F-38 | **A visibility guard that only looks at controls styled by a call NAMED `inputStyle` cannot see the defect it exists to catch.** F-27's scan matched `inputStyle`/`textareaStyle`/… by name, so it was blind both to controls styled with an ad-hoc literal (`style={{ padding: … }}`) and to controls styled through an alias (`customerInputStyle()`). Six controls across three pages — including all three fields on the customer's workspace settings page — were transparent rectangles on a white card while the test passed. | Phase 2C-A demo alignment | The rule is now the simple one it should always have been: every control carries `.bs-control`, exempting only the input types a browser draws itself. Sixteen hard-coded font sizes were found the same way, and now have a guard of their own. |
| F-39 | **A flex item defaults to `flex-shrink: 1`, and that is enough to obscure a link.** The sidebar foot took three wrong answers before the right one: `margin-block-start: auto` did nothing while the rail took its height from its content; a fixed height with an internal scroll put "Sign out" below the fold of a column nobody thinks to scroll; a sticky foot brought it back and then overlapped the last navigation item. Every symptom had the same cause — a ~840px navigation inside a 760px column was being squeezed while its content overflowed through everything below it. | Phase 2C-A demo alignment | The rail refuses to shrink and the page scrolls, which is what the reference does. axe named it precisely both times ("partially obscured, smallest space is 244.9px by 8.4px", "safe clickable space has a diameter of 10.8px"); the lesson is that a WCAG 2.5.8 target-size failure is usually a layout bug, not a sizing one. |
| F-40 | **Every Control Center screen showed "Overview" as the current page.** `AdminShell` marked the active nav item with `(activePath ?? '') === item.href`, and the console LAYOUT — which renders the shell — never passed `activePath`, so the empty string matched the console root's empty href on all sixteen routes. A Next.js layout cannot read the pathname on the server. | Phase 2C-A fidelity pass | The shell is a client component and resolves the active item from `usePathname()` by longest match, so `/console/workspaces/<id>` belongs to the workspaces item and `/console` cannot swallow it. A caller that sets `item.active` explicitly still wins. The same resolution supplies the top-bar title for routes whose layout never sees their heading. |
| F-41 | **Hiding a control's only text takes its accessible name with it.** Collapsing the rail to 78px hid the workspace switcher's and the sign-out button's labels; each was then a button whose only remaining child was an `aria-hidden` icon. axe reported `button-name` immediately. The related trap is subtler: the reference collapses this copy with `opacity: 0; width: 0`, which leaves it in the accessibility tree — announced but unreachable, the worst of both. | Phase 2C-A fidelity pass | `display: none` rather than a zero-width element, and both controls carry an explicit `aria-label` so the workspace and the exit are still named when their text is not shown. |
| F-42 | **The overflow suite went blind again — from the other side.** Reproducing the demo's shell (`.app-shell { overflow: hidden }`, for its 34px corners) and its scrolling panel (`.main-panel { overflow: auto }`) gave the page two ancestors that FIT the viewport and clip their own content. `inlineEndOverhang` is defined to forgive exactly that, so a planted 900px element measured 0 and the meta-assertion failed. Same shape as F-35: geometry that is correct for the design silently removes the check. | Phase 2C-A complete fidelity pass | The clip stays — it is the demo's, and it is what makes the corners real — and a SECOND measurement was added rather than the first being loosened. `clippedInlineOverflow` reports `scrollWidth - clientWidth` for a named container, and the per-width loop now asserts it is zero for both `.bs-shell` and `main` at every breakpoint in both directions. The self-check plants twice: once inside the panel, to prove the new measurement catches what the old one cannot, and once on `body`, to prove the old one still reports and names an offender. |
| F-43 | **Moving a control into a menu breaks every test that clicked it directly.** Sign-out moved into the profile card's `•••` menu, which is where the demo puts it. Both suites clicked `[data-testid="sign-out"]` with no menu open and timed out. | Phase 2C-A complete fidelity pass | The tests open the menu first. The assertion is unchanged — signing out must end the session — because the contract did not change, only the route to the control; a test rewritten to assert less would have been the wrong fix. |
| F-44 | **A test hook can be the only thing naming who is signed in.** The Control Center's rail carried a standalone `actor-identity` row; replacing it with the demo's profile card removed the element, and "shows who is signed in" failed with the operator's identity plainly visible on screen. | Phase 2C-A complete fidelity pass | `ProfileCard` takes a `nameTestId`, so the hook points at the one element that actually answers the question. The alternative — re-adding hidden markup for the test — would have made the assertion true of something the reader never sees. |
| F-45 | **A translucent stage lets the ambient orbs set the contrast.** With the auth page's card floating directly on the orbs, the "forgot password" link measured 4.12:1 (`#7935FE` on a `#F9D9A5` blend) and axe reported it serious. The demo does not have this problem because its `.auth-stage` is an OPAQUE wash and its footnote sits INSIDE the card — ours had the footnote outside it, on the orbs. | Phase 2C-A complete fidelity pass | Both fixed toward the demo rather than away from it: the stage takes the demo's opaque gradient, the card takes its `rgba(255,255,255,.9)` on top of it, and the footnote moves inside the card where `.auth-card > small` lives. A contrast failure whose fix is also the more faithful reproduction is worth noticing — it usually means the departure caused it. |
| F-46 | **This container's Playwright browser is not where Playwright looks for it.** `pnpm test:e2e` failed 195 tests in 2ms each with "Executable doesn't exist at `/opt/pw-browsers/chromium_headless_shell-1234/...`". Nothing was wrong with the code: the image ships `chromium-1194` and a `/opt/pw-browsers/chromium` symlink, and the config only honours it when `PLAYWRIGHT_CHROMIUM_EXECUTABLE` is set. | Phase 2C-A complete fidelity pass | Run with `PLAYWRIGHT_CHROMIUM_EXECUTABLE=/opt/pw-browsers/chromium`. Recorded because the failure mode — every test failing instantly with an identical error — reads like a catastrophic regression and is not one. |
| F-47 | **A `'use server'` module may export nothing but async functions, and violating it does not fail the build.** `plans/actions.ts` re-exported a synchronous helper. Next.js accepted the build and then could not resolve any action id from that module, so every form on the plans page posted into a void. The symptom was a client error boundary with no server-side stack — the least diagnosable shape a failure can take. | Phase 3 implementation (found by the E2E suite) | The re-export is gone. The residual risk is the PATTERN: nothing yet fails the build when a non-async export is added to an action module. A lint rule, or a structural test of the kind F-25 produced, would close it. |
| F-48 | **`??` does not catch the empty string an untouched text input actually posts.** The change-reason fallback used `String(form.get('reason') ?? 'default')`, so a blank field passed `""` to a Configuration Service that requires eight characters — and an operator who left an optional-looking field alone got "that change was rejected. Check the values and try again." The same operator error appeared twice more: a matrix cell rendered EMPTY when a grant carried `enumValue: ""`. | Phase 3 implementation | A blank-aware `reasonFrom()` helper, and a named `describeGrant()` that cannot return an empty string. The field now states the eight-character requirement rather than springing it. Worth generalising: for form input, `??` is almost never the operator you want — the absent case and the blank case are the same case. |
| F-49 | **Validating a raw configuration document is not the same as validating the parsed one.** Semantic checks ran against the payload as submitted, where a field the author omitted is `undefined` rather than its schema default. "A boolean feature takes no limit" therefore fired on every plan that had never mentioned a limit at all. | Phase 3 implementation (found by a unit test) | `validateConfiguration` now parses first and runs the semantic stage against the parsed document, which removes the whole class rather than the one instance. |
| F-50 | **A CHECK constraint caught an ordering bug that no amount of reading would have.** Settlement decremented a bucket's remaining balance before releasing its hold, so a bucket whose entire remainder was both held and spent transiently violated `reserved <= remaining` and PostgreSQL refused the write. | Phase 3 implementation (found by the credit-protocol suite) | Both movements are now one update, which is what they always logically were. Recorded because the instinct on seeing a constraint violation is to question the constraint: here the constraint was right and the code was wrong, and relaxing it would have allowed a bucket to be over-drawn under exactly the interleaving it exists to prevent. |
| F-51 | **A `data-testid` prefix selector catches the form as well as the table.** `[data-testid^="grant-"]` matched the matrix cells AND the editor's own `grant-plan`, `grant-limit` and `grant-enabled` controls, so an assertion about rendered cells was measuring an empty number input. | Phase 3 implementation | Scoped to `[data-testid^="matrix-"] [data-testid^="grant-"]`. A prefix that reads naturally in one place is a namespace collision in another; scope the selector rather than renaming every hook. |
| F-52 | **A `.env` reconstructed field-by-field is a different environment.** Building a fresh-database env by deriving `DATABASE_URL` from `DATABASE_MIGRATION_URL` ran the whole isolation suite as the table OWNER rather than the application role, producing 78 failures that looked like a Phase 3 regression and were not. | Phase 3 verification (my own error, recorded so the next person does not repeat it) | Derive the fresh env by substituting only the DATABASE NAME in the real one (`sed 's#/brandspace_test#/brandspace_fresh#g'`), never by reassembling it from parts. The suite then passed 469/469 on a migrations-only, unseeded database — which is what that verification was supposed to measure. |
| F-53 | **An unbounded admin listing plus test suites that never clean up degrades until it times out.** The secrets E2E test began failing at its ten-second ceiling. The action was working — the secret was written every time — but `listSecrets` has no `take`, and the local test database had accumulated **853 secret records** across repeated local runs (each isolation run adds ~36 probe secrets; each E2E run adds one). Rendering and axe-scanning that table is what exceeded the timeout; the query itself was 3.8ms. I first recorded this as a flake and re-ran it, which was wrong: it failed a second time, and a second failure is not a flake. | Phase 3 verification (found by root-causing rather than re-running) | Clearing the residue restored the suite to 72/72 in that project. **Two real issues remain and are NOT fixed here, deliberately.** (1) `SecretService.listSecrets` is unbounded — a genuine defect that will reach production, but the correct fix is pagination with a "showing N of M" affordance, and that is a redesign of a Phase 2A page this phase was told not to undertake. Adding a silent cap would be worse than the current behaviour: an operator would stop seeing secrets with no indication. (2) Neither the isolation nor the E2E suite cleans up what it creates, so any long-lived database drifts toward this. CI is unaffected — it builds a fresh database per run — which is exactly why it went unnoticed. **Recommended as the first item of Phase 4 housekeeping.** **CLOSED in the pre-Phase-4 hardening pass.** Both halves are fixed and proven. (1) `listSecrets` returns one PAGE — two bounded queries, a total order with an `id` tie-breaker, server-side search and filtering, URL-held state, an out-of-range page that recovers to the last page, and `Showing N–M of Total` in both languages. The page-size cap bounds one REQUEST, not what an operator may see: `total` always reports the true count and every record stays reachable, which is the distinction that made a silent cap unacceptable. Contract in `docs/ADMIN-CONTROL-CENTER.md` §7.1. (2) Every suite that writes secrets now carries a per-run token and removes exactly its own rows; strategy in `docs/ARCHITECTURE.md` §3.9. Proven by two consecutive full Playwright runs and two consecutive isolation runs leaving the `secret_record` count unchanged at 46, where each run previously added rows. No product-facing secret deletion path was added and no privilege was granted — cleanup uses the `DELETE` the platform role already held. |
| F-54 | **A fixture that fabricates an impossible state produces a failure in the code that is right.** The isolation fixtures created a `CreditReservation` claiming an allocation of 1000 against a `CreditGrant` whose `reservedMilliCredits` was 0 — a state the product cannot produce, because reserving always increments the bucket. The sweeper then could not release it: the decrement would take `reserved` below zero and the CHECK constraint refused, aborting the whole batch. Surfaced only because rows accumulate across runs and eventually pass their deadline, so the suite passed for a while and then did not. | Phase 3 verification (found by the full isolation run) | The fixture now records the hold. Separately, `sweepAbandonedReservations` no longer lets one unreleasable row stop the sweep: it reports the bad ids and continues, because reservation leaks are a metric that must stay at zero and a sweeper that dies on the first bad row stops releasing every valid reservation behind it. The constraint was NOT relaxed — it was correct, and relaxing it would have allowed a bucket to be over-drawn under exactly the interleaving it exists to prevent. |
| F-55 | **Catching a unique violation inside a PostgreSQL transaction does not work.** A failed statement ABORTS the surrounding transaction, so a `try/catch` around a racing `INSERT` leaves every later statement failing with "current transaction is aborted" — the catch runs and achieves nothing. | Phase 3 adversarial review | Wallet creation on first credit movement uses `INSERT … ON CONFLICT DO NOTHING`, which does not abort. Asserted by five concurrent first movements against a workspace with no wallet. |
| F-56 | **Prisma reports WHICH constraint a `P2002` violated in a different place depending on the driver.** The idempotency recovery matched `meta.target`, which the pg adapter does not populate — it nests the constraint under `meta.driverAdapterError.cause.constraint.index`. So the recovery never fired, and a raced retry surfaced a 500 for work that had actually succeeded. | Phase 3 adversarial review (found by writing the test for the fix) | Both shapes are checked, structurally and never on a message. Worth generalising: a `catch` written against an error shape nobody has printed is a `catch` that has not been tested. Twenty concurrent calls with one idempotency key now assert that none is rejected, not merely that the counter reads 1 — the weaker assertion passed throughout. |
| F-57 | **A chevron named for a logical direction still pointed at a physical one.** `ChevronStartIcon` and `ChevronEndIcon` are drawn as left- and right-pointing paths, and three components — the pagination, the sidebar collapse toggle, the calendar's month stepper — each carried a comment saying the glyph "flips with direction rather than pointing the wrong way in Arabic". Nothing flipped it. An Arabic reader saw "previous" pointing forwards. | Pre-Phase-4 hardening (found by looking at the rendered Arabic page, not at the code) | The two logical chevrons now carry `.bs-chevron-logical`, mirrored under `html[dir='rtl']` in `tokens.css`; only those two opt in, so an arrow that means something physical still points where it points. Asserted three ways in `tests/unit/design-system.test.ts` (both icons opt in, the rule exists, nothing else opts in) and once in the browser against the COMPUTED transform — a class with no rule behind it passes a source check and still renders backwards. Recorded because the comment was the defect's best camouflage: it had been read several times as evidence the behaviour was correct. |
| F-58 | **Replacing a per-test unique token with a per-RUN one collides with itself.** Wiring cleanup into `platform-services.test.ts` replaced nine `provider: \`p${Date.now()}\`` values with one shared token, and four tests that had been creating distinct refs started creating the same one — "a secret with ref … already exists. Rotate it instead." | Pre-Phase-4 hardening (found by running the whole isolation suite rather than the new files) | The token is a suffix on the original per-test letter, not a replacement for it: `${TEST_SECRET_PROVIDER}-p`, `-e`, `-r` and so on. Cleanup matches on the token as a substring, so ownership is unaffected. The lesson is narrower than it looks: a fixture identifier usually encodes two things — which RUN owns it and which TEST made it — and a change that only preserves the first breaks the second silently until the ref happens to be reused. |
| F-59 | **Pre-fix residue is not cleaned by the fix.** The local test database still holds 44 secret records created before this pass, plus the 2 the seed legitimately creates for MFA. Cleanup deletes only rows carrying a run token, and refuses everything else by design, so it cannot and must not remove them. | Pre-Phase-4 hardening | Left in place deliberately, and harmless now that the listing is paginated — they are what the "no accumulation" measurements are taken against. A developer who wants a clean local database should reset it (`pnpm db:reset`), which is an operator action, not something a test helper may do on its own initiative. CI is unaffected: it builds a fresh database per run. |
| F-60 | **The secrets page was not the only unbounded or silently-capped listing — it was the one that got large enough to fail.** Confirmed while fixing F-53, and left unfixed here deliberately, since this pass was scoped to F-53 alone. `WorkspaceService.list`takes 200 rows with no total and no navigation, which is precisely the *silent display cap* F-53 rejected: an operator with 201 customers simply stops seeing one, with nothing on screen to say so.`InvitationService`caps at 200 the same way.`ConfigurationService.#workspaceUsage`loads every workspace with a plan, unbounded, and grows with the customer base. The audit log's`take: 100`is honest by comparison — the page says "the 100 most recent events" — but still offers no way to reach an older one. | Pre-Phase-4 hardening (found by auditing every`findMany`while fixing F-53) | **Not fixed here.** The pagination contract in`docs/ADMIN-CONTROL-CENTER.md`§7.1 is deliberately written to be reusable, and`Pagination`in`packages/ui`is now correct in both directions, so applying it to the workspaces, invitations and audit listings is mechanical rather than a redesign. Sized as a small housekeeping item, to be scheduled by the owner rather than folded into Phase 4's scope. The risk is real but slow: unlike secrets, none of these grows from test runs. |
| F-61 | **Secrets were the accumulating rows that hurt; they are not the only accumulating rows.** Measured on a fresh migrations-only database: two consecutive full isolation runs leave **0 secrets** (the F-53 fix) and **20 credit reservations per run**, plus `platform_user`, `workspace`, `audit_event`and`configuration_version`rows. The reservations come from`createIsolationFixtures`, which gives each fixture workspace an OPEN reservation expiring in one hour; an hour later every one of them is an abandoned reservation, which is the supply line feeding F-62. | Pre-Phase-4 hardening (measured, not estimated) | Out of scope for a pass told to fix F-53 and its test-data problem: cleaning the fixture's reservations means a teardown in every suite that calls `createIsolationFixtures`, not one hook. `credit-protocol.test.ts` — the suite whose own residue was breaking it — does now delete the reservations for the workspaces it creates. For the rest, the token-and-`afterAll`pattern in`tests/support/secret-fixtures.ts` generalises directly; the guard that refuses a malformed token is the part to copy, not the part to skip. |
| F-62 | **`sweepAbandonedReservations` can be starved indefinitely by rows it cannot release.** It reads a bounded window (`take: 200`) of OPEN reservations past their deadline, with **no ordering**, and a row whose bucket does not record the hold it claims can never be released — the decrement would take `reserved`below zero and a CHECK constraint refuses it, correctly. Those rows are reported in`failed`and left OPEN, so they come back in the next window, and the next. Once as many as`limit`of them exist, the sweep releases **nothing**, for ever: every valid reservation behind them leaks, and`docs/BILLING-AND-CREDITS.md`§10.3 makes reservation leaks a metric that must stay at zero. Observed directly — the local test database holds 259 abandoned reservations of which the first 200 are all unreleasable, and one sweep pass swept 0. The supply is F-61: every fixture workspace gets an OPEN reservation expiring in an hour, so each run adds twenty rows that become abandoned by themselves. The absent`orderBy`also makes which rows a sweep touches arbitrary. | Pre-Phase-4 hardening (found by root-causing an intermittent sweeper test failure rather than re-running it — the failure was a real signal about the sweeper, not about the test) | **FIXED in the post-Phase-3 audit remediation — see A-7.** It was correctly out of scope for the F-53 pass, which is why it was recorded rather than carried silently. The suite's own contribution IS fixed:`credit-protocol.test.ts`now deletes the reservations for the workspaces it created, so it stops adding to the pile. Proposed fix, for the owner to schedule: bound the sweep by reservations RELEASED rather than rows EXAMINED — page past a window of failures instead of stopping inside it — and give the query a deterministic`orderBy: { expiresAt: 'asc' }`so a leak is cleared oldest-first. Recorded, not silently carried: unlike F-53's residue, this one can reach production, where the unreleasable rows come from real interleavings rather than a test fixture. |
| F-63 | **The gateway does not persist model output by default, so an idempotent replay can return nothing.** docs/AI-GATEWAY.md §7.4 guarantee 2 says a duplicate key returns the ORIGINAL result; §11 says prompts and responses are not persisted by default. Both cannot hold for a client that lost its response unless the result was kept somewhere. | Phase 4 (W6) | Resolved as an opt-in: `ai.routing`gains`parameters.persistOutput`, default **false**, writing to a nullable `ai_request.outputPayload`. A caller that stores its own artifact leaves it off. **RESOLVED 2026-09-13 by D-78**, which approved the default-off rule and required a defined retention and deletion policy for anything persisted. `outputRetentionDays`is now mandatory alongside`persistOutput`(validation refuses them apart) and`purgeExpiredOutputs()`clears expired payloads while retaining the usage, cost, credit, idempotency and audit metadata the policy requires be kept. **Remaining, and scheduled:** the customer-facing *notice* §11 describes is a dashboard surface and belongs with the Phase 5 feature that first turns persistence on. |
| F-64 | **Provider cost was typed as an integer count of MINOR units and would have recorded zero for every text request.** A model at $0.15 per million input tokens costs 0.015 of a cent per thousand tokens. In whole cents that is 0, so`providerCostMinor`would have been 0 on essentially every request and the margin reporting those columns feed would have shown infinite margin on every row — the one number the D-15 margin floor exists to catch. | Phase 4 (W5), found while writing the pricing tests | Fixed forward in migration`20260913090000`: both columns renamed to `providerCostMicroMinor`and widened to BIGINT, existing values multiplied so nothing changed worth. docs/AI-GATEWAY.md §4 and §7.2 corrected. The field name in the original document was the source of the mistake. |
| F-65 | **A stuck AI request whose reservation row was already gone could never leave`RUNNING`.** The release threw, the failure was never recorded, and the sweeper handed the same unfixable row back on every pass — the F-62 starvation shape, in which the reservation-leak metric of §12 would never return to zero. | Phase 4 (W7), found by the sweeper's own tests | A genuine `NOT_FOUND` from the release is tolerated and the request still reaches a terminal status; every other error still propagates. Nothing in production deletes a reservation row, so this is a belt-and-braces path, but an unfixable row in a bounded sweep is not a hypothetical cost. |
| F-66 | **Platform-wide provider spend caps (docs/AI-GATEWAY.md §8, last row) are not enforced.** Per-request, per-workspace and per-plan ceilings are. | Phase 4 (W8) | Deliberately deferred. Enforcing a platform-wide cap needs a rollup table, not a cross-tenant aggregate scan on the path of every request, and there is no real provider to spend against until D-13 is answered. **Scheduled** for the phase that adds the first real adapter. |
| F-67 | **Mid-trial plan changes grant no additional credits.** Carried forward from the post-Phase-3 audit at the owner's instruction. | Post-Phase-3 audit; owner decision recorded 2026-09-09 | **KEEP the current behaviour.** Phase 4 exposed no correctness problem with it: the gateway reserves and settles against whatever the wallet holds, and never assumes a grant accompanied a plan change. Recorded as an **owner-review item, not a Phase 4 blocker**. |
| A-1 | **A password reset reactivated any account that held a token.**`completePasswordReset`wrote`status: 'ACTIVE'`unconditionally, so a SUSPENDED user — or a DELETED one holding a token minted before the deletion — could resurrect their own account from a link in their inbox. It was also four separate statements: anything failing after consumption left the token spent and the password unchanged, and an invalid password was rejected AFTER the token was burned. | Independent post-Phase-3 audit | Only PENDING advances; SUSPENDED and DELETED are refused with the same message as an unknown token, and no token is minted for them. The whole reset is one transaction, and the password is hashed before anything is consumed. Six of nine new assertions fail against the previous implementation. **Fixed.** |
| A-2 | **A genuinely new invitee could not get in at all.**`accept`requires an authenticated identity and there is no sign-up route, so an invited address with no account reached the page, was told to sign in, and had nothing to sign in with. The journey only worked if somebody created the`User`row by hand — which the E2E seed did, with a comment saying the invitee "needs an account to accept with". That pre-creation is what hid the dead end for two phases. | Independent post-Phase-3 audit |`acceptAsNewUser`: the invitee sets a password, the identity is created from the address the INVITATION names, and the membership is granted in one transaction. The token is the proof of address control — it was delivered to that mailbox and the database holds only its hash. Uniform failures, token hashing, tenant isolation and single-use acceptance are all preserved, and both entry points now share one implementation of the conditional UPDATE so the guarantee cannot drift. The E2E test drives the journey for an address with only an invitation seeded. **Fixed.** |
| A-3 | **Assigning a plan wrote a string.** `assignPlan`set`workspace.planKey`and an audit event and stopped. No`WorkspaceSubscription`, so no status, no billing period, no trial, no pinned price and none of the credits the plan promises. The Plan & Usage page already read the subscription and there was never one to read; the cycle worker had nothing to advance. | Independent post-Phase-3 audit | Everything the assignment implies now happens in one transaction: plan key, subscription, price and configuration version pinned from the catalogue (AC-04.7), the trial when the plan offers one and the workspace has never had one, and the matching credit grant. Idempotency is derived from the subscription's PERIOD, never the clock — the first attempt keyed it on a freshly computed period end, which differs on every call. Changing plan mid-trial grants nothing, or an operator could mint credits by toggling. Unassigning CANCELS rather than deletes: the row is the evidence of what was sold. **Fixed.** |
| A-4 | **Three pieces of entitlement configuration looked live and were inert.** (1) `enabledForPlans`was declared on`FlagRule`, carried through the schema and the Control Center, and read by NOTHING — an operator could enable a feature for a plan, see it saved, and change nobody's experience. (2) `valueType: 'enum'`existed and no enum could ever resolve: the decision carried`enabled`and`limitValue`only, so an enum feature resolved to`defaultValue === true`, false for any string. (3) `dependsOn`was consulted only when an override was WRITTEN, so a dependency turned off afterwards left the dependent resolving to enabled for ever. | Independent post-Phase-3 audit |`enabledForPlans`resolves as an allow list (a non-match falls through rather than deciding against, or targeting one plan would be an outage for the rest). The decision carries`enumValue`, plans and overrides supply one, and `enumOptions`makes it validatable; overrides gained a nullable column. The dependency graph is walked on EVERY resolve, an override included, with cycles terminating closed. Twelve of sixteen new assertions fail against the previous implementation. **Fixed.** |
| A-5 | **Two owners could be removed at once, leaving a workspace with none.** The last-owner invariant is a COUNT, and a count takes no locks. Two concurrent demotions write DIFFERENT membership rows so nothing conflicts, and under READ COMMITTED each still sees the other owner as active. Both counted one, both passed, both committed. Textbook write skew, invisible to every sequential test. | Independent post-Phase-3 audit | The rows being written are not the rows being read, so row locks and stricter counts do not help.`changeRole`and`remove`take`FOR UPDATE`on the WORKSPACE row first — a mutex over the set — always in that order, so two queue rather than interleave and cannot deadlock. Per workspace, with a test asserting two unrelated workspaces still demote concurrently. **Fixed.** |
| A-6 | **The cycle boundary was four transactions and could double-charge on retry.** Expiry, forfeiture, the grant and the timestamps each committed separately; a crash between any two left a workspace half-reset. Retrying was worse than doing nothing: the grant was keyed on the cycle and no-opped, but the forfeiture was keyed per BUCKET, so a second pass ran FIFO against the survivors and forfeited again. Separately, the rollover cap was computed against a balance INCLUDING reserved credits and taken from`remaining − reserved`; `allocateFifo`returned a shortfall nobody read, so the customer silently kept credits above the cap. | Independent post-Phase-3 audit | One transaction behind a single completion marker keyed on the cycle. The shortfall is clamped and reported as`unforfeitable`, because credits held against an in-flight request cannot be seized without breaking it — the honest outcome is to take what can be taken and say how much could not. **Fixed.** |
| A-7 | **F-62, closed.** See F-62 for the defect. | Independent post-Phase-3 audit | Deterministic `[expiresAt asc, id asc]`ordering;`limit`bounds RELEASES rather than rows examined, with failures excluded and the next batch fetched; and the result reports`attempted`and`exhausted`— the difference between "no leak left" and "hit the bound, more to do", which a bare count cannot express. An explicit`maxAttempts`is honoured verbatim; the default gets a floor of one batch, because`limit * 4`for a small limit gives up before looking at anything. **Fixed.** |
| A-8 | **A retried settle or release surfaced a 500 for work that had succeeded.** Both read the reservation status through an UNLOCKED findUnique, so two concurrent calls both saw OPEN and both proceeded. They serialised on the wallet lock, by which point the loser was committed to writing: it decremented every hold a second time and reused the winner's idempotency key, hitting a CHECK violation or a P2002. | Independent post-Phase-3 audit | The constraints were right — they are what stopped the double charge. What was wrong is that a retry is a normal event and must be a no-op. The reservation row is now locked before its status is read, so the loser waits, sees a terminal status and returns. Eight concurrent settles, eight concurrent releases, and settle racing release each resolve to exactly one outcome with no rejections. **Fixed.** |
| A-9 | **An idempotency key named any request wearing it, not one request.**`grant`returned the stored bucket for ANY caller — so workspace B replaying A's key was handed A's bucket and told the grant had happened.`consume`returned the CALLER's consumption and skipped the increment, so a key reused across workspaces meant B's usage was silently never recorded: free quota, invisible in the counters precisely because the branch writes nothing.`refund`interpolated`amount`straight into`usedValue - ${amount}`, so a NEGATIVE refund incremented the counter, and had no idempotency key at all. | Independent post-Phase-3 audit | The immutable request fields — workspace, type or feature, and amount — now decide whether a replay is the same request; a mismatch is a CONFLICT. Refunds require a positive whole number and their own key, and are recorded as a negative `UsageEvent`. Every one of the eight new assertions fails against the previous implementation. **Fixed.** |
| A-10 | **Security-material changes were not atomic with their audit events, and support mode could overlap.** Only invitation ACCEPTANCE was audited: the customer's Activity Log showed people arriving with no record of who invited them, and a revocation left no trace. Support mode's `start`was three statements under a comment asserting one live session per operator per workspace, which nothing enforced — two concurrent starts produced two live grants whose accesses cannot be attributed to either. A failure after the INSERT left a LIVE SUPPORT GRANT WITH NO AUDIT EVENT. | Independent post-Phase-3 audit | Created, resent and revoked each write an event inside the same transaction as the change; a resend names the invitation it superseded, and no event carries the token. Support mode is one transaction, takes the workspace row`FOR UPDATE`, and is backed by a partial unique index on `(workspaceId, platformUserId) WHERE endedAt IS NULL`— the guarantee that holds if a future caller forgets the lock. Atomicity is proven by an injected failure: a trigger refusing the audit insert, installed as the migrator because the platform role correctly has no DDL rights. **Fixed.** |
| A-11 | **The workspace directory was the silent cap F-53 rejected, surviving.**`WorkspaceAdminService.list`took 200 rows with no total and no navigation. Three smaller caps (invitations at 200, support history at 50, the audit log at 100) stated nothing, or stated a claim rather than a number. | Independent post-Phase-3 audit | The directory is paginated on the F-53 contract. The other three now show "the most recent N of Total", which is the difference between a bounded read and a hidden one. Full pagination of the AUDIT LOG is **scheduled, not done**: it is append-only and platform-wide, so paging it usefully needs filters (actor, action, workspace, date range) that are their own piece of work. Invitations and support history are **scheduled** on the same basis, and are bounded per workspace rather than platform-wide, so the cap is not reachable in practice the way the directory's was. |
| A-12 | **The isolation fixtures manufactured fake credit leaks.**`createIsolationFixtures`gave every fixture workspace a reservation expiring in one hour, so each became an abandoned reservation sixty minutes later — twenty per full isolation run, accumulating for ever. That was the supply line behind F-62. | Independent post-Phase-3 audit remediation (measured: 0 secrets and 20 reservations per run on a fresh database) | The deadline is a century out, so the row stays for the isolation tests and leaves the abandoned set entirely. A fixture that puts noise into the one metric docs/BILLING-AND-CREDITS.md §10.3 requires to stay at zero is wrong even once the sweeper survives it. **Remaining, and scheduled:** the fixtures still leave`platform_user`, `workspace`, `audit_event`and`configuration_version` rows behind, which cost disk rather than correctness — nothing reads those under a bound. The token-and-`afterAll`pattern in`tests/support/secret-fixtures.ts` generalises directly when it becomes worth doing. |

### Phase 5B-1 additions (Asset Library)

| ID                    | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Raised in                                         | Recommendation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | When                                                         |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| **F-77**              | **No storage vendor is approved, so production cannot accept an upload.** The `ObjectStore` boundary is provider-agnostic and the filesystem implementation behind it is real, durable and cross-process, which is enough for development, test and CI. It is not enough for production: a container disk does not survive a deployment. D-105 makes this fail closed rather than silently lossy.                                                                                                                                                                                                                                                                                                                                                                                                                            | Phase 5B-1 (`packages/storage`)                   | **Owner decision.** Pick an object-storage provider and a data region consistent with D-03, then write the adapter behind the existing interface. No code outside `packages/storage` should need to change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Before any production release that exposes the Asset Library |
| **F-78**              | **No production virus scanner is selected.** Quarantine, the CLEAN gate, the failure path and the audit trail are all real and tested against `MockVirusScanner`; the thing that decides CLEAN is not. Until one is configured, production refuses uploads (D-106) — so this and F-77 gate the same release.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Phase 5B-1 (`packages/assets/src/scanning.ts`)    | **Owner decision.** Choose between a hosted scanning API and a self-managed engine, and record the licence, the update channel and where the bytes go. A hosted API means customer files leave the platform, which is a privacy decision, not only a technical one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Same release as F-77                                         |
| **F-79**              | **No image encoder has been reviewed, so no derivative bytes are produced.** `ENCODABLE_KINDS` is empty by design (D-107). Every candidate decodes untrusted bytes in native code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Phase 5B-1 (`packages/assets/src/derivatives.ts`) | Review one encoder properly — maintenance, licence, CVE history, offline installation, and whether it can be sandboxed or resource-capped. Then add the kind to `ENCODABLE_KINDS` and let the existing worker stage do the rest.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | When thumbnails are needed by a consuming module             |
| ~~F-80~~ **RESOLVED** | **Phase 5A left three PLAIN foreign keys that act as cross-tenant existence oracles.** `brand_source_chunk.sourceDocumentId`, `brand_ingestion_job.sourceDocumentId` and `brand_brain_message.conversationId` reference a parent by id alone. PostgreSQL evaluates referential integrity with RLS BYPASSED, so a write referencing another workspace's row SUCCEEDS where it should be rejected, and the difference between success and a constraint error tells the caller whether that id exists. Probed directly against the test database: all three confirmed. The Asset Library avoids it entirely (D-99).                                                                                                                                                                                                             | Phase 5B-1, while designing D-99                  | **Done** (F-80 corrective pass). Migration `20260914200000_f80_brand_brain_composite_foreign_keys` replaces all three with `(workspaceId, sourceDocumentId)` and `(workspaceId, conversationId)` against new `(workspaceId, id)` uniques on `brand_source_document` and `brand_brain_conversation`; `ON DELETE CASCADE` is preserved and no row is written, rewritten or deleted. A pre-flight anti-join refuses the migration — naming tables and counts, never ids — rather than repairing, because a cross-tenant row is an incident and not a cleanup task (D-113 explains why the pre-flight has to lift FORCE RLS to see anything at all, and what would otherwise have shipped: a constraint marked valid over data that violates it). **Evidence:** `tests/isolation/f80-brand-brain-composite-keys.test.ts` (21 tests) proves the three refusals from inside the caller's OWN workspace, that a real foreign id and a fabricated one fail with an IDENTICAL SQLSTATE, constraint and message — so the oracle is closed rather than moved — and that the relationships and their cascades still work. `tests/isolation/f80-migration-upgrade.test.ts` (9 tests) builds a database at main, DEMONSTRATES the defect by posting into another tenant's conversation successfully, shows the migration refusing while that row exists and changing nothing, then upgrading cleanly with every valid row preserved by id — and proves a migrations-only database has zero drift. | **Done**                                                     |
| **F-81**              | **SVG is refused (D-108).** Customers do upload logos as SVG, and telling them the format is unsupported is a real product gap, not only a security stance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Phase 5B-1 (`packages/assets/src/file-safety.ts`) | If SVG is wanted, it needs a sanitiser with the same dependency review F-79 describes, plus serving from an isolated origin or with a restrictive CSP. Do not add the type without both.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Owner decision                                               |
| **F-82**              | **Asset Library artwork is a design-system EXTENSION, not a port.** The approved demo routes `media` to a placeholder, so under D-98 the screen was built from the platform's own tokens, components and interaction patterns and recorded in `docs/UI-FIDELITY-CONTRACT.md` §6.3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Phase 5B-1 (`apps/dashboard`)                     | The owner may refine it during the F-76 UI parity pass. The extension is a starting point, not a claim that the design is settled.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | F-76 parity pass                                             |
| ~~F-83~~ **RESOLVED** | **The rest of Brand Brain carried FIVE more plain foreign keys of exactly the F-80 shape** — not the two first recorded here. The original entry was written after querying four tables; querying the whole module found `brand_knowledge_candidate.sourceDocumentId`, `brand_knowledge_candidate.targetItemId`, `brand_knowledge_item.sourceDocumentId`, `brand_knowledge_item.conflictsWithItemId` and `brand_knowledge_version.knowledgeItemId`. Two reach further than any F-80 key: `targetItemId` crosses the D-65 GOVERNANCE boundary (it names the approved item a candidate would EDIT, so a foreign id pointed the review diff at another tenant's canonical knowledge and an acceptance would have written against it), and `knowledgeItemId` attaches APPEND-ONLY history, which nothing can correct afterwards. | F-80 corrective pass                              | **Done** (same change as F-80, migration `20260914200000_f80_brand_brain_composite_foreign_keys`). All five are composite on `workspaceId` against `brand_source_document(workspaceId, id)` and the new `brand_knowledge_item(workspaceId, id)`. **Referential actions preserved exactly**, which for the three `SET NULL` keys required naming the column: a bare composite `ON DELETE SET NULL` nulls EVERY referencing column including `workspaceId`, which is `NOT NULL`, so the parent delete would have failed outright — measured, not assumed (D-114). **Evidence:** the catalogue test now asserts the whole module has NO single-column key to a tenant-owned parent, plus each key's preserved `ON DELETE` and each `SET NULL` key's `confdelsetcols`; the upgrade suite demonstrates the governance defect at main (A's candidate targets B's approved item and SUCCEEDS), proves both `SET NULL` deletes keep `workspaceId`, and proves an item with history still cannot be deleted because append-only wins.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Done**                                                     |
